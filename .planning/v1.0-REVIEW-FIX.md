---
phase: v1.0-milestone
fixed_at: 2026-05-18T00:00:00Z
review_path: .planning/v1.0-REVIEW.md
iteration: 1
findings_in_scope: 13
fixed: 6
skipped: 7
status: partial
---

# v1.0 Milestone: Code Review Fix Report

**Fixed at:** 2026-05-18
**Source review:** .planning/v1.0-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope (CR + WR): 13
- Fixed: 6
- Skipped: 7

---

## Fixed Issues

### CR-01: `setError` called inside `useMemo` — React rule violation

**Files modified:** `src/components/Projects/PaceCalculator/paceCalculator.jsx`
**Commit:** 991e53c
**Applied fix:** Removed `setError("")` from the `calc` `useMemo` callback. Added a dedicated `useEffect` with dependency array `[distance, hh, mm, ss]` that clears the error whenever relevant inputs change. The `useMemo` is now a pure computation.

---

### WR-04: `topArtistsMap.jsx` — `useEffect` missing cleanup for GeoJSON fetch

**Files modified:** `src/components/Projects/TopArtistsMap/topArtistsMap.jsx`
**Commit:** 3db5a05
**Applied fix:** Added an `AbortController` inside the GeoJSON fetch `useEffect`. The fetch now passes `{ signal: controller.signal }` and the cleanup function calls `controller.abort()`. The `.catch` handler ignores `AbortError` to avoid spurious console errors on normal unmount.

---

### WR-08: `500.jsx` — `window.confirm` blocks render

**Files modified:** `src/components/Projects/500/500.jsx`
**Commit:** 3042757
**Applied fix:** Added `showResetConfirm` state. `resetScores()` now sets this flag to `true`. The Reset button is replaced by an inline "Reset all scores? / Yes, reset / Cancel" confirmation UI that toggles based on the flag. `confirmReset()` performs the actual state clear.

---

### WR-09: `paceCalculator.jsx` — `autoAdvance` creates new function instances on every render

**Files modified:** `src/components/Projects/PaceCalculator/paceCalculator.jsx`
**Commit:** 3c695bd
**Applied fix:** Converted `autoAdvance` from a plain `function` declaration to a `const` wrapped in `useCallback` with an empty dependency array `[]`. Added `useCallback` to the React import. The function identity is now stable across renders.

---

### WR-10: `trainGame.jsx` — solution rows use index-based keys

**Files modified:** `src/components/Projects/TrainGame/trainGame.jsx`
**Commit:** d9cc614
**Applied fix:** Changed `key={i}` to `key={sols[i]}` in the solution row loop. Solution strings are unique within a result set, making this a stable and semantically correct key.

---

### WR-11: `NBA-Ladder.jsx` — `conferences[0]` fallback returns `undefined` on empty array

**Files modified:** `src/components/Projects/NBA-Ladder/NBA-Ladder.jsx`
**Commit:** e523c06
**Applied fix:** Replaced `conferences.find(...) || conferences[0]` with `conferences.find(...) ?? null`. The nullish coalescing operator only falls back on `undefined`/`null`, and the explicit `null` fallback is honest — the existing guard on line 97 (`!selectedConference`) correctly handles this case.

---

## Skipped Issues

### CR-02: Pervasive direct state mutation in `trioGame.jsx`

**File:** `src/components/Projects/Trio/trioGame.jsx:250-972`
**Reason:** Trio files excluded by user instruction.
**Original issue:** Class component mutates `this.state` directly throughout, bypassing React reconciliation.

---

### WR-01: `trioGame.jsx` — potential infinite loop in `pile_refresh()`

**File:** `src/components/Projects/Trio/trioGame.jsx:233-244`
**Reason:** Trio files excluded by user instruction.
**Original issue:** `while (card_changes === 0)` loop has no upper-bound guard; could hang the browser tab if a card code is missing from `original_deck`.

---

### WR-02: `trioGame.jsx` — `CPU_find_trio` accesses out-of-bounds array indices

**File:** `src/components/Projects/Trio/trioGame.jsx:930`
**Reason:** Trio files excluded by user instruction.
**Original issue:** Loop accesses `CPU_hand[count_through_hand+2]` without ensuring the array has at least 3 elements, risking a TypeError late in a game.

---

### WR-03: `trioGame.jsx` — wrong help modal content (copy-paste from Arbitrage)

**File:** `src/components/Projects/Trio/trioHome.jsx:37-62`
**Reason:** Trio files excluded by user instruction.
**Original issue:** Help modal body contains Arbitrage betting content instead of Trio game instructions.

---

### WR-05: `topArtistsMap.jsx` — `refreshAuthState` may cause infinite re-fetch

**File:** `src/components/Projects/TopArtistsMap/topArtistsMap.jsx:81-88`
**Reason:** Already correct — no fix needed. `refreshAuthState` is wrapped in `useCallback` in `SpotifyAuthProvider.jsx` (line 34), so its reference is stable and does not cause the effect to re-run on every render.
**Original issue:** If `refreshAuthState` were not memoised, it would produce a new function reference each render, causing continuous re-fetching.

---

### WR-06: `trioHome.jsx` — navigation via `window.location.href` breaks React Router

**File:** `src/components/Projects/Trio/trioHome.jsx:23,27`
**Reason:** Trio files excluded by user instruction.
**Original issue:** `onClick={() => window.location.href='TrioGame'}` causes full page reloads and bypasses React Router.

---

### WR-07: `trioGame.jsx` — `window.location.href` navigation on game over

**File:** `src/components/Projects/Trio/trioGame.jsx:556,906`
**Reason:** Trio files excluded by user instruction.
**Original issue:** Game-over conditions navigate via `window.location.href='/Projects'` causing full-page reloads.

---

_Fixed: 2026-05-18_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
