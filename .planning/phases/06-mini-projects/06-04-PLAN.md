---
phase: 06-mini-projects
plan: 04
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/components/Projects/NoisyDetector/noisyDetector.jsx
  - src/components/Projects/NoisyDetector/noisyDetector.css
autonomous: true
requirements:
  - TOOL-06

must_haves:
  truths:
    - "NoisyDetector is a functional component"
    - "All commented-out Bootstrap modal blocks and RNSoundLevel code are deleted"
    - "The component renders an empty div with no errors or warnings"
    - "noisyDetector.css is deleted"
    - "npm run build passes"
  artifacts:
    - path: "src/components/Projects/NoisyDetector/noisyDetector.jsx"
      provides: "Trivial functional stub component"
      exports: ["default NoisyDetector"]
  key_links:
    - from: "src/components/main.jsx"
      to: "src/components/Projects/NoisyDetector/noisyDetector.jsx"
      via: "route /Projects/Noise"
      pattern: "NoisyDetector"
---

<objective>
Convert NoisyDetector from a class component to a trivial functional component, delete all commented-out Bootstrap and React Native code (dead code cleanup per D-07), and delete noisyDetector.css.

Purpose: NoisyDetector is a stub — its active render is just `return <div />`. The class scaffolding, commented-out Bootstrap modals, and commented-out RNSoundLevel code are dead weight. The simplest valid migration is a 5-line functional component. Per D-07.
Output: Rewritten `noisyDetector.jsx` as a minimal functional component; `noisyDetector.css` deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-mini-projects/06-CONTEXT.md
@.planning/phases/06-mini-projects/06-RESEARCH.md

<interfaces>
<!-- Target implementation (from RESEARCH.md §NoisyDetector) -->
The entire component becomes:

  import React from 'react';

  const NoisyDetector = () => {
    return <div />;
  };

  export default NoisyDetector;

No other imports needed. No state. No lifecycle. The empty div is the intended behaviour.

<!-- noisyDetector.css (all rules are dead code per RESEARCH.md) -->
All rules (.noisy-wrapper, .noisy-title, #noisy-image, .solutions, .error, .app-image)
are only referenced in commented-out JSX. File is safe to delete entirely.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite noisyDetector.jsx as minimal functional stub</name>
  <files>src/components/Projects/NoisyDetector/noisyDetector.jsx</files>
  <read_first>
    - src/components/Projects/NoisyDetector/noisyDetector.jsx (read current file to confirm all code is either dead or commented)
    - .planning/phases/06-mini-projects/06-RESEARCH.md §NoisyDetector (confirms render is empty div, all else is dead)
  </read_first>
  <action>
Replace the entire content of `noisyDetector.jsx` with the minimal functional stub below. Every line of the current file (class definition, constructor, commented-out imports, commented-out Bootstrap blocks, commented-out RNSoundLevel code, lifecycle methods) is replaced. The active render was `return <div></div>` — preserve that behaviour.

```jsx
import React from 'react';

const NoisyDetector = () => {
  return <div />;
};

export default NoisyDetector;
```

Also remove the import line `import './noisyDetector.css'` if present (it may already be absent — confirm during read). Per D-07 and D-15.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File does NOT contain `class NoisyDetector extends`
    - File does NOT contain `import.*react-bootstrap`
    - File does NOT contain `import.*react-native-sound-level`
    - File does NOT contain `import.*noisyDetector.css`
    - File does NOT contain `componentDidMount` or `componentWillUnmount`
    - File does NOT contain `this.state` or `this.setState`
    - File contains `const NoisyDetector = ()` or `function NoisyDetector(`
    - File contains `export default NoisyDetector`
    - File is under 10 lines
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>noisyDetector.jsx is a minimal 5-line functional component returning an empty div, all dead code removed, and build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Delete noisyDetector.css</name>
  <files>src/components/Projects/NoisyDetector/noisyDetector.css</files>
  <read_first>
    - src/components/Projects/NoisyDetector/noisyDetector.jsx (confirm import already removed in Task 1)
  </read_first>
  <action>
Delete `src/components/Projects/NoisyDetector/noisyDetector.css`. All CSS rules in this file reference class names that are only used in commented-out JSX (dead code). The import line has been removed from noisyDetector.jsx in Task 1. Per D-07 and D-15.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/Projects/NoisyDetector/noisyDetector.css` does NOT exist
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>noisyDetector.css deleted, build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | NoisyDetector renders an empty div. No user input, no API calls, no state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Information Disclosure | Removed dead code | accept | Deleting commented-out code reduces attack surface — no secrets or logic were in the comments. Positive security change. |
</threat_model>

<verification>
After both tasks:
- `npm run build` exits 0
- Navigate to `/Projects/Noise` — page renders without error (blank page is correct behaviour for this stub)
- No console errors
</verification>

<success_criteria>
NoisyDetector is a trivial functional component, all commented-out dead code removed, noisyDetector.css deleted, and `npm run build` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-mini-projects/06-04-SUMMARY.md`
</output>
