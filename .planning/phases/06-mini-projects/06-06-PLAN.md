---
phase: 06-mini-projects
plan: 06
type: execute
wave: 3
depends_on:
  - 06-01
files_modified:
  - src/components/Projects/NBA-Ladder/NBA-Ladder.jsx
  - src/components/Projects/NBA-Ladder/nbaLadder.css
autonomous: true
requirements:
  - TOOL-07

must_haves:
  truths:
    - "All nba-* CSS class names replaced with Tailwind utility classes inline"
    - "Light-mode colour variables (var(--card-background, #fff), var(--text-color, #222)) are gone"
    - "No Bootstrap 'card' class remains on the root div"
    - "The 80px left margin is replaced with standard centred layout"
    - "The API_URL fetch call to /.netlify/functions/standings is preserved verbatim"
    - "nbaLadder.css is deleted"
    - "npm run build passes"
  artifacts:
    - path: "src/components/Projects/NBA-Ladder/NBA-Ladder.jsx"
      provides: "Functional NBALadder component with Tailwind dark styling"
      exports: ["default NBALadder"]
  key_links:
    - from: "NBA-Ladder.jsx"
      to: "/.netlify/functions/standings"
      via: "fetch(API_URL)"
      pattern: "/.netlify/functions/standings"
---

<objective>
Replace all `nba-*` CSS class names in NBA-Ladder.jsx with inline Tailwind utilities, flip the light-mode colour palette to dark-mode Tailwind tokens, remove the legacy 80px left margin, and delete nbaLadder.css.

Purpose: NBALadder uses light-mode CSS variables that render as a white box on the dark site. The 80px left margin is a legacy sidebar assumption. Both are corrected via inline Tailwind. The Netlify function API call must not change. Per D-09.
Output: NBA-Ladder.jsx with all Tailwind classes inline; nbaLadder.css deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-mini-projects/06-CONTEXT.md
@.planning/phases/06-mini-projects/06-RESEARCH.md

<interfaces>
<!-- API call (preserve exactly — TOOL-07 dependency) -->
const API_URL = '/.netlify/functions/standings'
const res = await fetch(API_URL)
Do NOT change this string or the fetch call in any way.

<!-- nba-* CSS class → Tailwind mapping (from RESEARCH.md §NBALadder) -->
root div (was "nba-ladder card"):
  Remove Bootstrap "card" class entirely.
  Tailwind: "bg-surface rounded-xl border border-white/10 p-4 m-4 max-w-[860px] mx-auto"
  NO margin-left: 80px equivalent (D-12 — legacy sidebar assumption removed)

nba-header     → "flex items-center justify-between gap-3 mb-3"
conf-tabs      → "flex gap-2"
conf-tab       → "bg-transparent border border-white/10 px-3 py-1.5 rounded-md cursor-pointer text-slate-300 hover:text-white"
conf-tab active → "bg-white/10 border-transparent text-white px-3 py-1.5 rounded-md cursor-pointer"
  (active vs inactive distinguished by conditional className)
table-wrap     → "overflow-x-auto rounded-lg mt-2"
nba-table      → "w-full border-collapse text-sm"
nba-loading    → "py-8 text-slate-400 text-center"
nba-error      → "py-8 text-slate-400 text-center"
nba-empty      → "py-8 text-slate-400 text-center"
team-name td   → "py-2.5 px-2 text-white overflow-hidden text-ellipsis"
team-w td      → "py-2.5 px-2 text-right text-white w-20 pr-4"
team-l td      → "py-2.5 px-2 text-right text-white w-20 pr-4"
team-pct td    → "py-2.5 px-2 text-right text-white w-20 pr-4"

Table header th cells: "py-2 px-2 text-left text-slate-400 text-xs uppercase tracking-wider font-semibold border-b border-white/10"
Table rows: alternate row shading "even:bg-white/5" on tr elements

Heading (title): "text-accent text-xl font-semibold m-0"
Season subtext (if any): "text-slate-400 text-sm"

<!-- Light-mode colour variables to eliminate -->
var(--text-color, #222)         → text-white (or text-slate-200)
var(--card-background, #fff)    → bg-surface (already on root div)
var(--muted-color, #374151)     → text-slate-400
var(--heading-color, #1f2937)   → text-accent (for headings)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all nba-* classes with Tailwind inline classes in NBA-Ladder.jsx</name>
  <files>src/components/Projects/NBA-Ladder/NBA-Ladder.jsx</files>
  <read_first>
    - src/components/Projects/NBA-Ladder/NBA-Ladder.jsx (MUST read full file before modifying)
    - src/components/Projects/NBA-Ladder/nbaLadder.css (read all rules to ensure complete coverage)
    - .planning/phases/06-mini-projects/06-RESEARCH.md §NBALadder (class mapping table and colour variable list)
  </read_first>
  <action>
Modify `NBA-Ladder.jsx` — it is already a functional component, so only change styling:

1. **Remove:** `import './nbaLadder.css'`

2. **Replace root div className:** The root was `className="nba-ladder card"`. Replace with:
   `className="bg-surface rounded-xl border border-white/10 p-4 m-4 max-w-[860px] mx-auto"`
   The `card` (Bootstrap) and `nba-ladder` classes are removed entirely. NO `margin-left: 80px` equivalent (D-12).

3. **Replace every `nba-*` and `conf-*` className** using the mapping table in the interfaces block. For the active/inactive conference tab: use conditional className — `className={activeConf === 'East' ? 'bg-white/10 border-transparent text-white px-3 py-1.5 rounded-md cursor-pointer' : 'bg-transparent border border-white/10 px-3 py-1.5 rounded-md cursor-pointer text-slate-300 hover:text-white'}`.

4. **Replace any inline `style` usages** that reference CSS variables (e.g., `style={{color: 'var(--text-color, #222)'}}`) with the Tailwind equivalents from the interfaces block.

5. **Preserve verbatim:**
   - `const API_URL = '/.netlify/functions/standings'`
   - All `fetch(API_URL)` logic
   - All state variables and their names
   - All error handling logic
   - All table rendering logic (just className changes, no logic changes)

6. **Table rows:** Add `className="even:bg-white/5"` to `<tr>` elements for subtle row striping (dark-mode appropriate).

7. **No `mt-14` or top margin** on any wrapper element (D-12).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File does NOT contain `import.*nbaLadder.css`
    - File does NOT contain `className="nba-ladder`
    - File does NOT contain `className=".*card"` (Bootstrap card class removed)
    - File does NOT contain `margin.*80px` or `marginLeft.*80`
    - File does NOT contain `var(--card-background` or `var(--text-color`
    - File contains `const API_URL = '/.netlify/functions/standings'` (exact string preserved)
    - File contains `bg-surface rounded-xl` on the root div
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All nba-* classes replaced with Tailwind dark-theme utilities, light-mode variables gone, API call unchanged, build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Delete nbaLadder.css</name>
  <files>src/components/Projects/NBA-Ladder/nbaLadder.css</files>
  <read_first>
    - src/components/Projects/NBA-Ladder/NBA-Ladder.jsx (confirm import removed in Task 1)
  </read_first>
  <action>
Delete `src/components/Projects/NBA-Ladder/nbaLadder.css`. All CSS rules have been replaced with Tailwind inline classes in Task 1. The import line has been removed. Per D-09 and D-15.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/Projects/NBA-Ladder/nbaLadder.css` does NOT exist
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>nbaLadder.css deleted, build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Netlify Function | Fetch to /.netlify/functions/standings which proxies Sportradar API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Spoofing | API_URL fetch response | accept | Response is display-only standings data — no PII, no mutations. Error state already implemented in component handles network failures. |
| T-06-06-02 | Denial of Service | Netlify function unavailability | accept | Component already has graceful error state (`nba-error` / `nba-loading` path). No retry loop. |
</threat_model>

<verification>
After both tasks:
- `npm run build` exits 0
- Navigate to `/Projects/NBA-Ladder` — component renders with dark Tailwind styling (no white box)
- Without `netlify dev` running: error/loading message displays gracefully
- Root div is centred (no 80px left offset)
- No double top spacing
</verification>

<success_criteria>
All nba-* CSS classes replaced with Tailwind dark-theme utilities, light-mode colour variables eliminated, legacy 80px left margin gone, API call preserved verbatim, nbaLadder.css deleted, and `npm run build` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-mini-projects/06-06-SUMMARY.md`
</output>
