---
phase: 06-mini-projects
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/components/Projects/TrainGame/trainGame.jsx
  - src/components/Projects/TrainGame/trainGame.css
autonomous: true
requirements:
  - TOOL-03

must_haves:
  truths:
    - "TrainGame is a functional component (no class syntax)"
    - "Bootstrap Modal and Button imports are gone"
    - "Entering a valid 4-digit number produces a solutions list"
    - "Entering an invalid value shows an error message"
    - "Help and Creation modals open and close correctly using the shared Modal"
    - "trainGame.css file is deleted"
    - "npm run build passes"
  artifacts:
    - path: "src/components/Projects/TrainGame/trainGame.jsx"
      provides: "Functional TrainGame component with Tailwind styling"
      exports: ["default TrainGame"]
  key_links:
    - from: "trainGame.jsx"
      to: "src/components/Shared/Modal.jsx"
      via: "import Modal from '../../Shared/Modal'"
      pattern: "import Modal from"
    - from: "trainGame.jsx"
      to: "./trainGameCalculations"
      via: "import getSolutions from './trainGameCalculations'"
      pattern: "getSolutions"
---

<objective>
Convert TrainGame from a class component to a functional component, replace Bootstrap Modal/Button with the shared Tailwind Modal and plain button elements, and delete trainGame.css.

Purpose: TrainGame uses Bootstrap Modal and Button imports that currently render as unstyled stubs via the vite.config.js Bootstrap stub. Converting to functional + Tailwind gives it working, styled modals and consistent visual treatment. Per D-05.
Output: Rewritten `trainGame.jsx` as a functional component; `trainGame.css` deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-mini-projects/06-CONTEXT.md
@.planning/phases/06-mini-projects/06-RESEARCH.md
@.planning/phases/06-mini-projects/06-01-SUMMARY.md

<interfaces>
<!-- Shared Modal (created in Plan 06-01) -->
Props: { title: string, onClose: () => void, children: ReactNode }
Import path from TrainGame: import Modal from '../../Shared/Modal'
Usage: {helpModalShow && <Modal title="Help" onClose={() => setHelpModalShow(false)}>...</Modal>}

<!-- trainGameCalculations (do NOT modify) -->
import getSolutions from './trainGameCalculations'
getSolutions(numberString) → array of solution strings/elements

<!-- Current CSS class → Tailwind mapping (from RESEARCH.md) -->
trainGame-wrapper  → "text-center text-accent min-h-screen"   (drop margin-top: 56px)
trainGame-title    → "pb-2 w-3/5 mx-auto"
solutions          → "pt-2 pb-40"
error              → "pt-2 text-red-500"
app-image          → "w-1/2"

<!-- Button style (Claude's discretion per CONTEXT.md) -->
Primary action buttons: "bg-accent text-black font-semibold px-4 py-2 rounded"
Submit button: same style

<!-- Inline styles to remove (RESEARCH.md §TrainGame) -->
style={{width: '100%', margin: 'auto'}} → remove (use w-full mx-auto)
style={{position:"absolute", left:"40%"}} → remove (use flex layout)
style={{width: '100%'}} → remove
style={{position:"absolute", right:"40%"}} → remove (use flex layout)
style={{height: '33px'}} → remove (use Tailwind h-9 or py-2)
style={{marginTop: '-3px'}} → remove (use items-center on flex row)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert trainGame.jsx — class to functional, Bootstrap to Tailwind Modal</name>
  <files>src/components/Projects/TrainGame/trainGame.jsx</files>
  <read_first>
    - src/components/Projects/TrainGame/trainGame.jsx (MUST read full file before rewriting)
    - src/components/Projects/TrainGame/trainGame.css (read to confirm all classes are covered)
    - src/components/Shared/Modal.jsx (confirm Modal interface — output of plan 06-01)
    - .planning/phases/06-mini-projects/06-RESEARCH.md §TrainGame (CSS class table, inline styles list, state variables, logic to preserve)
  </read_first>
  <action>
Rewrite `trainGame.jsx` as a functional component. Follow the pattern from RESEARCH.md §TrainGame functional conversion skeleton exactly. Key requirements:

1. **Remove:** `import { Component } from 'react'`, `import { Modal, Button } from 'react-bootstrap'`, `import './trainGame.css'`

2. **Add:** `import { useState } from 'react'`, `import Modal from '../../Shared/Modal'`

3. **State:** Replace `this.state` with five `useState` hooks:
   - `const [number, setNumber] = useState('')`
   - `const [error, setError] = useState('')`
   - `const [solutions, setSolutions] = useState([])`
   - `const [helpModalShow, setHelpModalShow] = useState(false)`
   - `const [madeModalShow, setMadeModalShow] = useState(false)`

4. **Logic:** Preserve `validateAndSubmit` and `onInputChange` logic verbatim — only convert `this.setState(...)` calls to the appropriate setters. The call `getSolutions(number)` from `./trainGameCalculations` must remain unchanged.

5. **Layout:** Header row using `className="flex items-center justify-between px-4 py-3"`:
   - Left: Creation button `<button onClick={() => setMadeModalShow(true)} className="bg-accent text-black font-semibold px-4 py-2 rounded">Creation</button>`
   - Centre: title in `<div className="pb-2 w-3/5 mx-auto">`
   - Right: Help button `<button onClick={() => setHelpModalShow(true)} className="bg-accent text-black font-semibold px-4 py-2 rounded">Help</button>`

6. **Input row:** `<input>` with `className="border border-accent/50 bg-transparent text-white rounded px-3 py-2 text-center"`, Submit button with same accent style, in a `flex items-center justify-center gap-2` wrapper.

7. **Error + solutions divs:** `<div className="pt-2 text-red-500">` and `<div className="pt-2 pb-40">` respectively.

8. **Root wrapper:** `<div className="text-center text-accent min-h-screen">` — NO mt-14 or margin-top (D-12).

9. **HelpModal content:** Use the shared Modal component:
   ```jsx
   {helpModalShow && (
     <Modal title="Help" onClose={() => setHelpModalShow(false)}>
       {/* preserve the exact help text content from the original file */}
     </Modal>
   )}
   ```

10. **MadeModal content:** Same pattern — preserve exact modal body content (two `<img>` elements with `className="w-1/2"`).

11. **Export:** `export default TrainGame`

Preserve all original modal content (text and images) verbatim.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File does NOT contain `import.*react-bootstrap`
    - File does NOT contain `class TrainGame extends`
    - File does NOT contain `this.state` or `this.setState`
    - File does NOT contain `import.*trainGame.css`
    - File contains `import Modal from '../../Shared/Modal'`
    - File contains `import getSolutions from './trainGameCalculations'`
    - File contains `const TrainGame = ()` or `function TrainGame(`
    - File contains `useState` (at least 5 usages)
    - File contains `getSolutions(` (calculation call preserved)
    - Root wrapper div does NOT contain `mt-14` or `margin-top`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>trainGame.jsx is a functional component, imports shared Modal, preserves all calculation logic and modal content, and the build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Delete trainGame.css</name>
  <files>src/components/Projects/TrainGame/trainGame.css</files>
  <read_first>
    - src/components/Projects/TrainGame/trainGame.jsx (confirm import line is already removed in Task 1)
    - src/components/Projects/TrainGame/trainGame.css (confirm no rules needed elsewhere)
  </read_first>
  <action>
Delete the file `src/components/Projects/TrainGame/trainGame.css`. All CSS class names from this file have been replaced with Tailwind inline classes in Task 1. The import line `import './trainGame.css'` has already been removed from `trainGame.jsx` in Task 1. Per D-05 and D-15.

After deleting, run `npm run build` to confirm no dangling import errors.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/Projects/TrainGame/trainGame.css` does NOT exist
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>trainGame.css is deleted and the build passes with no import errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → TrainGame input | User enters a 4-digit string in the number input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | TrainGame number input | accept | Input is validated by `validateAndSubmit` before passing to `getSolutions`. Invalid input shows an error message and does not call the calculation. No server-side processing — all client-side. Low value target. |
| T-06-02-02 | Information Disclosure | getSolutions output | accept | Output is purely client-side arithmetic — no PII, no secrets. |
</threat_model>

<verification>
After both tasks:
- `npm run build` exits 0
- `npm run lint` passes (no undefined variable errors from removed class methods)
- Navigate to `/Projects/TrainGame`, enter `2384` → solutions list appears
- Help and Creation modals open and close correctly
- No double top spacing (toolbar + mt-14 gap absent)
</verification>

<success_criteria>
TrainGame renders as a functional component with Tailwind styling, Bootstrap imports removed, shared Modal used for Help and Creation modals, trainGame.css deleted, and `npm run build` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-mini-projects/06-02-SUMMARY.md`
</output>
