---
phase: 06-mini-projects
plan: 08
type: execute
wave: 3
depends_on:
  - 06-01
files_modified:
  - src/components/Projects/500/500.jsx
autonomous: true
requirements:
  - TOOL-05

must_haves:
  truths:
    - "The StyleTag component and its JSX usage are deleted"
    - "The COLORS constant is deleted"
    - "All style={...} inline style object references are replaced with Tailwind className strings"
    - "White card aesthetic is preserved via explicit bg-white text-black classes"
    - "localStorage persistence (fivehundred-scorer-v1) is preserved verbatim"
    - "npm run build passes"
  artifacts:
    - path: "src/components/Projects/500/500.jsx"
      provides: "Functional 500 Scorer with Tailwind classes replacing COLORS and inline styles"
      exports: ["default FiveHundred (or 500 component)"]
  key_links:
    - from: "500.jsx"
      to: "localStorage"
      via: "usePersistentState hook"
      pattern: "fivehundred-scorer-v1"
---

<objective>
Delete the `StyleTag` component (which injects `color-scheme: light only` and breaks the dark site), delete the `COLORS` constant, and replace all inline style objects with Tailwind utility classes in 500.jsx. Preserve localStorage persistence and all game logic verbatim.

Purpose: The `StyleTag` overrides the site's `color-scheme: dark` with `color-scheme: light only`, conflicting with index.css. The COLORS object and inline styles become Tailwind utilities. The white card aesthetic is intentional and preserved via explicit `bg-white text-black`. Per D-11.
Output: Modified 500.jsx — same file, StyleTag deleted, COLORS deleted, inline styles replaced with Tailwind.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-mini-projects/06-CONTEXT.md
@.planning/phases/06-mini-projects/06-RESEARCH.md

<interfaces>
<!-- COLORS constant → Tailwind mapping (from RESEARCH.md §500 Scorer) -->
COLORS.primary     (#fcc001)  → bg-accent (or text-accent for text) — project accent token #ffc200 is close
COLORS.primaryDark (#b48700)  → border-yellow-600  (or keep as inline style if needed)
COLORS.surface     (#ffffff)  → bg-white
COLORS.ink         (#000000)  → text-black
COLORS.backdrop    (#333134)  → bg-surface
COLORS.success     (#16a34a)  → bg-green-600 text-white
COLORS.danger      (#dc2626)  → bg-red-600 text-white
COLORS.selectedFill (#ffef9c) → bg-yellow-100

<!-- StyleTag removal (CRITICAL — from RESEARCH.md §500 Scorer critical issues) -->
Delete the StyleTag component definition entirely.
Delete the <StyleTag /> usage in JSX.
This component injects: :root { color-scheme: light only; }
After removal, the site's dark color-scheme is restored.
The white card remains white because of explicit bg-white on the card elements.

<!-- White card preservation (RESEARCH.md §500 Scorer open question 3) -->
500 Scorer is intentionally a light-themed UI.
After StyleTag removal, apply explicit bg-white text-black to all card-level elements
to prevent the site's global text-white from bleeding in.
Do NOT dark-mode the card interior.

<!-- usePersistentState hook — preserve verbatim -->
Uses localStorage.getItem/setItem('fivehundred-scorer-v1')
Do NOT change the key name or the hook implementation.

<!-- useSmallScreen hook — simplify or keep -->
The breakpoint is at 520px.
Option A: Keep hook, replace computed style objects with conditional className strings.
Option B: Remove hook, replace with Tailwind sm: classes (sm: = 640px, close enough to 520px).
Use Option B (remove useSmallScreen, use sm: classes) to eliminate the matchMedia dependency.

<!-- Modal inside 500.jsx -->
The file contains a custom Modal component (not Bootstrap).
It can either be replaced with the shared Modal component or kept as inline Tailwind classes.
Per Claude's discretion — use the shared Modal for consistency since Plan 06-01 created it.
Import: import Modal from '../Shared/Modal'
(Note: 500.jsx is at src/components/Projects/500/500.jsx, so path is '../../Shared/Modal')

<!-- Inline styles in 500.jsx — general approach -->
The file uses a const styles object similar to PaceCalculator.
Delete the styles object. Replace each style={styles.X} with className="..." using COLORS → Tailwind mapping.
Key patterns:
  Bet row selected state: style={{background: COLORS.selectedFill}} → className="bg-yellow-100"
  Success button: style={{background: COLORS.success, color: COLORS.surface}} → className="bg-green-600 text-white"
  Danger button: style={{background: COLORS.danger, color: COLORS.surface}} → className="bg-red-600 text-white"
  Primary/accent button: style={{background: COLORS.primary}} → className="bg-accent text-black"
  Card wrapper: style={{background: COLORS.surface, color: COLORS.ink}} → className="bg-white text-black"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete StyleTag and COLORS, replace inline styles with Tailwind in 500.jsx</name>
  <files>src/components/Projects/500/500.jsx</files>
  <read_first>
    - src/components/Projects/500/500.jsx (MUST read full file — particularly StyleTag definition, COLORS object, styles object, usePersistentState hook, useSmallScreen hook)
    - src/components/Shared/Modal.jsx (confirm Modal interface for replacing inline Modal)
    - .planning/phases/06-mini-projects/06-RESEARCH.md §500 Scorer (COLORS mapping, StyleTag critical issue, white card preservation note)
  </read_first>
  <action>
Modify `500.jsx` — it is already a functional component. Key changes:

1. **Delete StyleTag entirely:**
   - Delete the `function StyleTag()` (or `const StyleTag`) component definition — it contains `<style>{':root { color-scheme: light only; }'}</style>` or similar.
   - Delete `<StyleTag />` from JSX (usually near the top of the render return).

2. **Delete the `COLORS` constant object** in its entirety.

3. **Delete the `styles` or `useSmallScreen` hook** and replace with Tailwind `sm:` responsive prefixes on affected elements.

4. **Replace every `style={...}` referencing COLORS or styles** with `className="..."` using the mapping from the interfaces block. Do this systematically — search for every `style=` usage and replace.

5. **Replace inline Modal** (if present inside 500.jsx):
   - Add `import Modal from '../../Shared/Modal'` at the top
   - Delete the inline Modal component definition
   - Replace `<InlineModal ...>` with `<Modal title="..." onClose={...}>...</Modal>`

6. **Preserve white card aesthetic:** Apply `className="bg-white text-black"` on all card-level wrapper elements. This prevents the site's global `text-white` from bleeding in after StyleTag removal.

7. **Preserve verbatim (DO NOT TOUCH):**
   - `usePersistentState` hook implementation and `'fivehundred-scorer-v1'` localStorage key
   - All game state variables
   - All game logic functions (addBet, undo, reset, scoring calculations)
   - All 500-game-specific rendering logic (just className changes, no logic changes)

8. **No `mt-14` on any wrapper** (D-12).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File does NOT contain `StyleTag` (definition or usage)
    - File does NOT contain `color-scheme: light` in any string
    - File does NOT contain `const COLORS` or `COLORS.` references
    - File does NOT contain `style={styles.` or `style={{background: COLORS`
    - File contains `'fivehundred-scorer-v1'` (localStorage key preserved)
    - File contains `bg-white text-black` on card elements (white card preserved)
    - File does NOT contain `mt-14` or `marginTop: 56` on any wrapper
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>StyleTag deleted, COLORS deleted, all inline styles replaced with Tailwind, white card preserved via explicit classes, localStorage hook unchanged, build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → localStorage | 500 Scorer reads/writes game state to localStorage key 'fivehundred-scorer-v1' |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08-01 | Tampering | localStorage game state | accept | localStorage is same-origin only. Game state is purely client-side score data — no PII, no secrets. Tampered localStorage would only affect the user's own game score. |
| T-06-08-02 | Information Disclosure | StyleTag removal | mitigate | Removing `color-scheme: light only` prevents conflicting style injection that could confuse browser colour rendering. Positive security/correctness improvement. |
</threat_model>

<verification>
After task completion:
- `npm run build` exits 0
- Navigate to `/Projects/FiveHundred` — white card renders on dark background
- Record a bet — bet appears in list with correct colour (bg-yellow-100 when selected)
- Click Undo — last bet removed correctly
- Click Reset — game state clears
- Refresh page — game state restores from localStorage
- Browser devtools: no `color-scheme: light only` in computed styles
</verification>

<success_criteria>
StyleTag deleted, COLORS constant deleted, all inline style references replaced with Tailwind classes, white card aesthetic preserved via explicit `bg-white text-black`, localStorage persistence unchanged, and `npm run build` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-mini-projects/06-08-SUMMARY.md`
</output>
