---
phase: 06-mini-projects
plan: 07
type: execute
wave: 3
depends_on:
  - 06-01
files_modified:
  - src/components/Projects/PaceCalculator/paceCalculator.jsx
autonomous: true
requirements:
  - TOOL-04

must_haves:
  truths:
    - "All inline style objects (styles.wrap, styles.card, etc.) are replaced with Tailwind className strings"
    - "The isSmall state and computed inlineRowStyle/actionsStyle objects are replaced with Tailwind sm: responsive classes"
    - "nextRef.current.focus() auto-tab behaviour is preserved via React.useRef"
    - "All calculation logic and URL state persistence (window.history.replaceState) is preserved verbatim"
    - "No CSS file exists or is imported (PaceCalculator had none)"
    - "npm run build passes"
  artifacts:
    - path: "src/components/Projects/PaceCalculator/paceCalculator.jsx"
      provides: "Functional PaceCalculator with Tailwind classes replacing inline styles"
      exports: ["default PaceCalculator"]
  key_links:
    - from: "paceCalculator.jsx"
      to: "window.history.replaceState"
      via: "URL state persistence"
      pattern: "history.replaceState"
---

<objective>
Replace the `styles` inline style object in paceCalculator.jsx with Tailwind className strings, eliminate the `isSmall` state variable in favour of Tailwind `sm:` responsive prefixes, and preserve all calculation logic and URL state persistence verbatim.

Purpose: PaceCalculator uses a large `styles` object with computed style values that are better expressed as Tailwind utilities. The `isSmall` `window.matchMedia` state can be removed entirely by using `sm:` responsive prefixes. Per D-10.
Output: Modified paceCalculator.jsx — same file, inline styles replaced with Tailwind classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-mini-projects/06-CONTEXT.md
@.planning/phases/06-mini-projects/06-RESEARCH.md

<interfaces>
<!-- styles object → Tailwind mapping (from RESEARCH.md §PaceCalculator) -->
styles.wrap       → "grid place-items-center p-8 sm:p-4 w-full"   (no background needed — inherits from body)
styles.card       → "bg-white border border-gray-200 rounded-2xl max-w-[760px] w-full shadow-lg text-gray-900"
styles.header     → "px-5 pt-5 pb-3"
styles.h2         → "m-0 text-2xl font-semibold text-gray-900"
styles.muted      → "mt-1.5 text-slate-500 text-sm"
styles.content    → "px-5 pb-5 grid gap-4"
styles.row        → "grid gap-2"
styles.label      → "font-semibold text-sm text-gray-700"
styles.input      → "bg-white text-gray-900 border border-gray-200 rounded-xl p-3.5 text-base outline-none w-full focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
styles.chips      → "flex gap-2 flex-wrap items-center"
styles.chip       → "inline-flex items-center gap-2 bg-white border border-gray-200 px-4 py-2 rounded-full cursor-pointer select-none text-gray-700 text-sm"
styles.chipChecked → add "border-blue-600 shadow-[0_0_0_4px_rgba(37,99,235,0.2)]" to chip when checked
styles.primaryBtn → "bg-blue-600 text-white border-0 rounded-xl p-3.5 text-base font-bold cursor-pointer w-full hover:bg-blue-700"
styles.secondaryBtn → "bg-transparent border border-gray-200 rounded-xl p-3.5 text-base font-bold cursor-pointer w-full text-gray-700 hover:border-gray-400"
styles.divider    → "h-px bg-gray-200 my-1"
styles.result     → "px-5 pb-5 pt-3 grid gap-2"
styles.pace       → "text-5xl font-extrabold text-gray-900"
styles.paceSup    → "text-[55%] text-slate-500 font-bold ml-1"
styles.detail     → "text-slate-500 text-sm"
styles.error      → "text-red-600 font-semibold text-sm"

<!-- isSmall state elimination -->
Remove: const [isSmall, setIsSmall] = useState(false) and any useEffect/matchMedia code
Remove: const inlineRowStyle = isSmall ? ... : ...
Remove: const actionsStyle = isSmall ? ... : ...
Replace with Tailwind sm: classes on the relevant elements:
  Row that stacks on mobile: "grid sm:grid-cols-2 gap-4" vs "grid-cols-1 gap-4"
  Actions that stack on mobile: "flex sm:flex-row flex-col gap-2"

<!-- nextRef preservation (from RESEARCH.md §PaceCalculator) -->
The autoAdvance function uses nextRef.current.focus() for auto-tabbing between time inputs.
Preserve the useRef hook and all nextRef usages exactly.
Do NOT remove refs.

<!-- URL state persistence (preserve verbatim) -->
window.history.replaceState(null, '', '?' + params.toString())
Preserve all URLSearchParams logic and history.replaceState calls exactly.

<!-- NOTE: Light card on dark background -->
PaceCalculator is intentionally a white card on the dark site background.
Use bg-white text-gray-900 explicitly on the card — do NOT dark-mode the card interior.
The outer wrap has no background (body bg-surface shows through, which is the intended contrast).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline styles object with Tailwind classes in paceCalculator.jsx</name>
  <files>src/components/Projects/PaceCalculator/paceCalculator.jsx</files>
  <read_first>
    - src/components/Projects/PaceCalculator/paceCalculator.jsx (MUST read full file — particularly the styles object, isSmall state, and nextRef usage)
    - .planning/phases/06-mini-projects/06-RESEARCH.md §PaceCalculator (complete styles object mapping, isSmall removal pattern)
  </read_first>
  <action>
Modify `paceCalculator.jsx` — it is already a functional component. Key changes:

1. **Delete the entire `const styles = { ... }` object** at the top of the component (or wherever it's defined).

2. **Delete `isSmall` state and its effect:**
   - Remove `const [isSmall, setIsSmall] = useState(false)`
   - Remove the `useEffect` that sets up the `matchMedia` listener for `isSmall`
   - Remove `inlineRowStyle` and `actionsStyle` computed variables
   - Remove corresponding `window.matchMedia` calls

3. **Replace every `style={styles.X}` usage** with `className="..."` using the exact Tailwind strings from the interfaces block above. Apply the mapping systematically — each `styles.X` reference becomes a `className`.

4. **Handle chipChecked conditional:**
   Replace `style={isChecked ? {...styles.chip, ...styles.chipChecked} : styles.chip}` with:
   `className={\`inline-flex items-center gap-2 bg-white border px-4 py-2 rounded-full cursor-pointer select-none text-gray-700 text-sm \${isChecked ? 'border-blue-600 shadow-[0_0_0_4px_rgba(37,99,235,0.2)]' : 'border-gray-200'}\`}`

5. **Handle responsive layout** (replacing isSmall logic):
   - Time input row: `className="grid gap-4 sm:grid-cols-3"` (3 cols on desktop, 1 on mobile)
   - Action buttons row: `className="flex flex-col sm:flex-row gap-2"`

6. **Preserve verbatim (DO NOT TOUCH):**
   - `nextRef` and all `useRef` calls
   - `autoAdvance` function body
   - All `window.history.replaceState` and `URLSearchParams` logic
   - All calculation functions (pace, distance, time derivations)
   - All event handlers
   - All state variables EXCEPT `isSmall`

7. **No `mt-14` on any wrapper** (D-12). The outer wrap has `className="grid place-items-center p-8 sm:p-4 w-full"` — no top margin.

8. **White card is intentional:** `className="bg-white border border-gray-200 rounded-2xl max-w-[760px] w-full shadow-lg text-gray-900"` on the card. Do NOT change to bg-surface or dark theme inside the card.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File does NOT contain `const styles = {` (styles object deleted)
    - File does NOT contain `style={styles.` (no more style object references)
    - File does NOT contain `isSmall` (state removed)
    - File does NOT contain `matchMedia` (window.matchMedia removed)
    - File does NOT contain `inlineRowStyle` or `actionsStyle`
    - File contains `nextRef` (ref preserved)
    - File contains `history.replaceState` (URL persistence preserved)
    - File contains `className="bg-white` on the card element
    - Root wrapper does NOT contain `mt-14` or `marginTop`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All inline styles replaced with Tailwind classes, isSmall state eliminated, URL state and refs preserved, build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → URL params | PaceCalculator persists state in URL query string via window.history.replaceState |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | Tampering | URL query string inputs | accept | URL params are read back as numeric strings and used in arithmetic. Invalid values produce NaN which the component handles. No server-side processing. Low value target. |
</threat_model>

<verification>
After task completion:
- `npm run build` exits 0
- Navigate to `/Projects/PaceCalculator` — white card renders on dark background
- Enter a distance and time, click Calculate → pace result displays correctly
- Resize to mobile width — layout stacks vertically without double top spacing
- Tab between time inputs — auto-advance focus works (nextRef preserved)
</verification>

<success_criteria>
All inline `styles` object references replaced with Tailwind className strings, `isSmall` state removed in favour of `sm:` responsive prefixes, URL state persistence and nextRef preserved verbatim, and `npm run build` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-mini-projects/06-07-SUMMARY.md`
</output>
