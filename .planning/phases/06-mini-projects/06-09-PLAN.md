---
phase: 06-mini-projects
plan: 09
type: execute
wave: 4
depends_on:
  - 06-01
  - 06-03
files_modified:
  - src/components/Projects/Trio/trioGame.jsx
  - src/components/Projects/Trio/trioGame.css
autonomous: true
requirements:
  - TOOL-02

must_haves:
  truths:
    - "TrioGame remains a class component — class syntax is NOT changed"
    - "Bootstrap Modal, Button, Carousel imports are removed"
    - "HelpModal function component uses the shared Modal with a step-based image navigator"
    - "MadeModal function component uses the shared Modal (but remains commented-out in the render)"
    - "All this.state properties, this.setState calls, and lifecycle methods are untouched"
    - "All game logic methods (shuffle, card_clicked, CPU_turn, discard1-13, etc.) are untouched"
    - "trioGame.css is kept and trimmed — only absolute-position ID rules remain"
    - "npm run build passes"
  artifacts:
    - path: "src/components/Projects/Trio/trioGame.jsx"
      provides: "TrioGame class component with Bootstrap removed, shared Modal + step indicator"
      exports: ["default TrioGame"]
    - path: "src/components/Projects/Trio/trioGame.css"
      provides: "Absolute-position game board layout (ID rules — required for gameplay)"
      contains: "#player_card_1, #pile, #deck, #play_button"
  key_links:
    - from: "trioGame.jsx HelpModal"
      to: "src/components/Shared/Modal.jsx"
      via: "import Modal from '../../Shared/Modal'"
      pattern: "import Modal from"
    - from: "trioGame.jsx"
      to: "trioGame.css"
      via: "import './trioGame.css'"
      pattern: "trioGame.css"
---

<objective>
Remove Bootstrap Modal, Button, and Carousel imports from trioGame.jsx, replace with the shared Tailwind Modal and plain button elements, replace the Carousel with a state-driven step indicator in HelpModal, and trim trioGame.css to keep only the absolute-position ID rules.

**CRITICAL:** TrioGame MUST remain a class component. Do NOT convert to functional. Do NOT touch `this.state`, `this.setState`, or any game logic method. Only Bootstrap JSX and CSS class names change. Per D-03, D-04.
Output: trioGame.jsx with Bootstrap removed; trioGame.css trimmed (NOT deleted).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-mini-projects/06-CONTEXT.md
@.planning/phases/06-mini-projects/06-RESEARCH.md
@.planning/phases/06-mini-projects/06-01-SUMMARY.md
@.planning/phases/06-mini-projects/06-03-SUMMARY.md

<interfaces>
<!-- Shared Modal (created in Plan 06-01) -->
Props: { title: string, onClose: () => void, children: ReactNode }
Import path from TrioGame: import Modal from '../../Shared/Modal'

<!-- TrioGame class structure to LEAVE UNTOUCHED (D-03, D-04) -->
- constructor() and this.state (all properties)
- shuffle(), instruction_change(), hand_refresh(), pile_refresh(), componentDidMount()
- pick_up_deck(), pick_up_pile(), move_cards_pos(), move_cards_pos_back()
- discard_initialise(), play_initialise(), rearrange_initialise()
- add_trio_1/2/3/4(), trio_refresh(), player_discard(), add_cards(), card_clicked()
- discard1() through discard13() (note: index quirks preserved as-is)
- CPU_turn(), CPU_find_trio(), CPU_change_for_joker()
- render() method structure and all this.state references within it

<!-- Bootstrap usages to replace (RESEARCH.md §TrioGame Bootstrap usages) -->
REMOVE: import { Modal, Button, Carousel } from 'react-bootstrap'
ADD:    import Modal from '../../Shared/Modal'
        import React from 'react'  (if not already imported — needed for React.useState in HelpModal)

Button replacements:
  Line 995: <Button className="made-button" ...>Creation</Button>
  → <button className="bg-accent text-black font-semibold px-4 py-2 rounded cursor-pointer" onClick={...}>Creation</button>

  Line 1002: <Button className="help-button" ...>Help</Button>
  → <button className="bg-accent text-black font-semibold px-4 py-2 rounded cursor-pointer" onClick={...}>Help</button>

HelpModal function component — full replacement (from RESEARCH.md §TrioGame Carousel replacement):
  Replace Bootstrap <Modal>, <Modal.Header>, <Modal.Body>, <Carousel>, <Carousel.Item> with:

  function HelpModal({ show, onHide }) {
    const [slide, setSlide] = React.useState(0);
    const total = 13;
    if (!show) return null;
    return (
      <Modal title="Help" onClose={onHide}>
        <div className="aspect-video mb-4">
          <iframe
            className="w-full h-full rounded"
            src="https://www.youtube.com/embed/[PRESERVE_ORIGINAL_URL]"
            title="Trio instructions"
            frameBorder="0"
            allowFullScreen
          />
        </div>
        <div className="flex flex-col items-center gap-4">
          <img
            src={`../Trio/Instruction_images/Trio - User Manual - ${slide + 1}.png`}
            alt={`Instruction ${slide + 1} of ${total}`}
            className="w-full"
          />
          <div className="flex items-center gap-4">
            <button
              onClick={() => setSlide(s => Math.max(0, s - 1))}
              disabled={slide === 0}
              className="px-3 py-1 bg-surface border border-white/20 rounded disabled:opacity-40"
            >←</button>
            <span className="text-sm text-slate-400">{slide + 1} / {total}</span>
            <button
              onClick={() => setSlide(s => Math.min(total - 1, s + 1))}
              disabled={slide === total - 1}
              className="px-3 py-1 bg-surface border border-white/20 rounded disabled:opacity-40"
            >→</button>
          </div>
        </div>
      </Modal>
    );
  }

  IMPORTANT: Preserve the original YouTube iframe src URL verbatim from the existing file.

MadeModal function component — full replacement:
  function MadeModal({ show, onHide }) {
    if (!show) return null;
    return (
      <Modal title="How this was made" onClose={onHide}>
        {/* preserve original MadeModal body content verbatim */}
      </Modal>
    );
  }
  Note: MadeModal is currently commented out in the render. Keep it commented-out.

<!-- CSS class name replacements in trioGame.jsx JSX (RESEARCH.md §TrioGame CSS migration) -->
className="trio-wrapper"
  → className="text-center min-h-screen"  (drop margin-top: 56px, D-12)

className="trio-title"  (the "Trio" heading)
  → className="text-accent text-2xl font-semibold py-3"

Any className="trio-button" or className="made-button" or className="help-button"
  → already handled by Button → <button> replacement above with explicit classes

className="video-container" and className="instruction-video"
  → replaced inline in HelpModal above

<!-- trioGame.css strategy (RESEARCH.md §TrioGame CSS) -->
KEEP: All absolute-position ID rules (#player_card_1 through #CPU_trio_2_card_8, #pile, #deck, #play_button, #play, etc.)
      These define the game board canvas and are REQUIRED for gameplay.
REMOVE from CSS file: .trio-wrapper, .trio-title, .trio-button:hover, .video-container, .instruction-video
      These are replaced by Tailwind inline classes.
KEEP: #play { background-color: #006400 } (dark green game table)
The import line 'import ./trioGame.css' REMAINS in trioGame.jsx (file is not deleted).

<!-- trioGame-state.jsx — DO NOT TOUCH -->
Leave src/components/Projects/Trio/trioGame-state.jsx completely unchanged.
Apply all Phase 6 changes only to trioGame.jsx.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Bootstrap from trioGame.jsx — replace with shared Modal, step indicator, and plain buttons</name>
  <files>src/components/Projects/Trio/trioGame.jsx</files>
  <read_first>
    - src/components/Projects/Trio/trioGame.jsx (MUST read full 1090-line file before modifying — note exact line numbers for Modal/Button/Carousel usages, YouTube iframe src URL, HelpModal and MadeModal definitions)
    - src/components/Shared/Modal.jsx (confirm Modal interface from plan 06-01)
    - src/components/Projects/Trio/trioGame.css (read to know which class rules will be removed in Task 2)
    - .planning/phases/06-mini-projects/06-RESEARCH.md §TrioGame (Bootstrap usages, Carousel replacement code, class name table)
    - .planning/phases/06-mini-projects/06-03-SUMMARY.md (confirm TrioHome Modal pattern for consistency)
  </read_first>
  <action>
Modify `trioGame.jsx`. This is a 1090-line class component. Work surgically — change ONLY the Bootstrap-related lines. Do NOT restructure the class.

**Step 1 — Update imports (top of file):**
- Remove: `import { Modal, Button, Carousel } from 'react-bootstrap'`
- Add: `import Modal from '../../Shared/Modal'`
- Ensure `import React from 'react'` or `import React, { Component } from 'react'` is present (needed for React.useState in HelpModal)
- Keep `import './trioGame.css'` — CSS file is kept (trimmed in Task 2)

**Step 2 — Replace HelpModal function component** (find the existing `function HelpModal(...)` definition):
Replace the entire function body with the step-based navigator from the interfaces block.
CRITICAL: Extract the YouTube iframe src URL from the current file and preserve it verbatim in the new iframe.
The slide state uses `React.useState` (not destructured useState) because HelpModal is a function component inside a class component file.

**Step 3 — Replace MadeModal function component:**
Replace the Bootstrap Modal pattern with the shared Modal. Preserve body content verbatim. MadeModal usage in render stays commented-out (that's the existing state — do not uncomment).

**Step 4 — Replace Button elements in the class render() method:**
- Line ~995: `<Button className="made-button" ...>Creation</Button>` → `<button className="bg-accent text-black font-semibold px-4 py-2 rounded cursor-pointer" onClick={this.setState...}>Creation</button>`
  (preserve the exact onClick handler — it calls this.setState)
- Line ~1002: `<Button className="help-button" ...>Help</Button>` → same pattern

**Step 5 — Replace CSS class names in render():**
- `className="trio-wrapper"` → `className="text-center min-h-screen"` (NO margin-top, D-12)
- `className="trio-title"` → `className="text-accent text-2xl font-semibold py-3"`
- Any remaining `className="trio-button"` → add `cursor-pointer` directly to the element

**Step 6 — DO NOT TOUCH anything else:**
- All `this.state.*` references
- All `this.setState(...)` calls
- All method bodies (shuffle, card_clicked, CPU_turn, etc.)
- All ID-based JSX attributes (`id="player_card_1"`, `id="pile"`, etc.)
- The `render()` return structure beyond the class/button changes above
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File does NOT contain `import.*react-bootstrap`
    - File does NOT contain `<Carousel` or `<Carousel.Item`
    - File does NOT contain `<Modal.` (Bootstrap Modal sub-components)
    - File DOES contain `class TrioGame extends` (class component preserved)
    - File DOES contain `this.state` (state preserved)
    - File DOES contain `this.setState` (setState preserved)
    - File DOES contain `import Modal from '../../Shared/Modal'`
    - File DOES contain `import './trioGame.css'` (CSS import kept)
    - File DOES contain `React.useState` in HelpModal (step indicator state)
    - File DOES contain `Trio - User Manual - \${slide + 1}.png` (instruction images)
    - Root wrapper div does NOT contain `mt-14` or `marginTop: 56`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Bootstrap removed from trioGame.jsx, HelpModal uses step indicator with shared Modal, MadeModal uses shared Modal, game logic untouched, build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Trim trioGame.css — remove class-based rules, keep absolute-position ID rules</name>
  <files>src/components/Projects/Trio/trioGame.css</files>
  <read_first>
    - src/components/Projects/Trio/trioGame.css (read full file — identify which rules are ID-based positions vs class-based)
    - src/components/Projects/Trio/trioGame.jsx (confirm class names removed in Task 1 are no longer referenced in JSX)
  </read_first>
  <action>
Edit `trioGame.css` — do NOT delete the file. Remove only the class-based rules that have been replaced by Tailwind inline classes in Task 1. Keep ALL absolute-position ID rules intact.

**Remove from CSS file:**
- `.trio-wrapper { ... }` (replaced by `text-center min-h-screen` in Task 1)
- `.trio-title { ... }` (replaced by `text-accent text-2xl font-semibold py-3` in Task 1)
- `.trio-button:hover { cursor: pointer }` (replaced by `cursor-pointer` Tailwind class)
- `.video-container { ... }` (replaced inline in HelpModal)
- `.instruction-video { ... }` (replaced inline in HelpModal)
- Any other class-based rules that are no longer referenced in JSX

**KEEP in CSS file (do NOT remove):**
- ALL `#player_card_*` rules (absolute positions for player hand cards)
- ALL `#CPU_card_*` rules (absolute positions for CPU hand cards)
- ALL `#CPU_trio_*_card_*` rules (absolute positions for trio slots)
- `#pile { ... }` (position of discard pile)
- `#deck { ... }` (position of draw deck)
- `#play_button { ... }` (position of play/action button)
- `#play { background-color: #006400; ... }` (dark green game table)
- Any other `#id` rules that define the game board geometry

The CSS file remains imported by trioGame.jsx — this import is intentional and must not be removed.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/Projects/Trio/trioGame.css` EXISTS (not deleted)
    - File does NOT contain `.trio-wrapper`
    - File does NOT contain `.trio-title`
    - File does NOT contain `.trio-button`
    - File does NOT contain `.video-container`
    - File DOES contain `#player_card_` (absolute position rules preserved)
    - File DOES contain `#pile` or `#deck` (game board rules preserved)
    - File DOES contain `#play` with background-color (game table colour preserved)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>trioGame.css trimmed — class rules removed, absolute-position ID rules preserved, file kept, build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → TrioGame click handlers | User interacts with card elements via click events that trigger this.setState calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09-01 | Tampering | trioGame.jsx state mutations | accept | The direct state mutations (this.state.hand.push, this.state.deck.splice) are intentional legacy patterns. They manipulate only client-side game state. No server submission. Preserved verbatim per D-03/D-04. |
| T-06-09-02 | Denial of Service | 1090-line class component complexity | accept | No new attack surface introduced. CSS-only change. Regression risk mitigated by preserving all logic verbatim. |
</threat_model>

<verification>
After both tasks:
- `npm run build` exits 0
- Navigate to `/Projects/TrioGame` — game board renders on dark green canvas
- Click "Help" → shared Modal opens with step indicator showing instruction image 1/13
- Click → and ← arrows to navigate instruction pages — all 13 images load
- Click "Creation" (if visible and functional in current codebase state)
- Play a full game hand — cards respond to clicks, CPU takes turns, scoring works
- No double top spacing
- `npm run build && npm run lint` passes (wave-level gate)
</verification>

<success_criteria>
TrioGame remains a class component with all game logic intact, Bootstrap imports removed, HelpModal uses shared Modal with 13-page step indicator, trioGame.css trimmed to absolute-position ID rules only (not deleted), and `npm run build` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-mini-projects/06-09-SUMMARY.md`
</output>
