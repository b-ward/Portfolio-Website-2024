---
phase: 06-mini-projects
plan: 05
type: execute
wave: 3
depends_on:
  - 06-01
files_modified:
  - src/components/Projects/Arbitrage/arbitrage.jsx
  - src/components/Projects/Arbitrage/arbitrage.css
autonomous: true
requirements:
  - TOOL-01

must_haves:
  truths:
    - "All arb-* CSS class names are replaced with Tailwind utility classes inline"
    - "The InfoModal uses the shared Modal component"
    - "No Bootstrap classes remain (no 'card' class either)"
    - "arbitrage.css import is removed and the file is deleted"
    - "getArbitrageBets import and all calculation logic is preserved verbatim"
    - "npm run build passes"
  artifacts:
    - path: "src/components/Projects/Arbitrage/arbitrage.jsx"
      provides: "Functional Arbitrage calculator with Tailwind styling"
      exports: ["default Arbitrage"]
  key_links:
    - from: "arbitrage.jsx"
      to: "./arbitrageCalcutations"
      via: "import getArbitrageBets from './arbitrageCalcutations'"
      pattern: "getArbitrageBets"
    - from: "arbitrage.jsx InfoModal"
      to: "src/components/Shared/Modal.jsx"
      via: "import Modal from '../../Shared/Modal'"
      pattern: "import Modal from"
---

<objective>
Replace all `arb-*` CSS class names in arbitrage.jsx with inline Tailwind utility classes (using the exact mapping table from RESEARCH.md), replace the InfoModal with the shared Modal component, and delete arbitrage.css.

Purpose: Arbitrage is already a functional component with no Bootstrap. This is a CSS class-name substitution task. The 429-line CSS file is entirely replaced by inline Tailwind utilities. Per D-08.
Output: arbitrage.jsx with all Tailwind classes inline; arbitrage.css deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-mini-projects/06-CONTEXT.md
@.planning/phases/06-mini-projects/06-RESEARCH.md
@.planning/phases/06-mini-projects/06-01-SUMMARY.md

<interfaces>
<!-- Shared Modal (created in Plan 06-01) -->
Props: { title: string, onClose: () => void, children: ReactNode }
Import path from Arbitrage: import Modal from '../../Shared/Modal'
The existing InfoModal already uses { title, children, onClose } — it's a drop-in replacement.

<!-- Calculation import (preserve verbatim) -->
import getArbitrageBets from './arbitrageCalcutations'
Note: "arbitrageCalcutations" has a typo in the filename — preserve this typo exactly.

<!-- Full arb-* → Tailwind mapping (from RESEARCH.md §Arbitrage) -->
arb-page               → "min-h-screen p-5 text-white"   (NO margin-top, D-12)
arb-header             → "flex items-center justify-between gap-4 mb-5"
arb-title              → "text-accent text-2xl font-semibold text-center flex-1 m-0"
arb-header-actions     → "flex-none"
arb-link-button        → "bg-transparent border border-accent/50 text-accent rounded-lg px-3.5 py-1.5 text-sm font-semibold cursor-pointer hover:bg-accent/10 hover:border-accent"
arb-controls           → "text-center mb-6"
arb-fetch-button       → "border border-accent bg-accent text-black rounded-lg px-5 py-2.5 text-base font-bold cursor-pointer disabled:opacity-65 disabled:cursor-not-allowed"
arb-loading-state      → "inline-flex items-center gap-2"
arb-spinner            → "w-3.5 h-3.5 rounded-full border-2 border-black/35 border-t-black animate-spin"
arb-loading-hint       → "mt-3 mx-auto max-w-[480px] text-slate-400 text-sm"
arb-error              → "text-red-400 bg-red-400/10 border border-red-400/35 rounded-lg px-4 py-3 mb-4"
arb-results            → "max-w-[900px] mx-auto"
arb-diagnostics        → "mb-3 text-slate-400 text-sm bg-slate-400/8 border border-slate-400/25 rounded-lg px-3 py-2"
arb-tabs               → "flex border-b-2 border-accent/25 mb-5"
arb-tab                → "bg-transparent border-0 border-b-2 border-transparent -mb-0.5 px-5 py-2.5 text-slate-400 text-base font-semibold cursor-pointer flex items-center gap-2 hover:text-accent"
arb-tab--active        → add "text-accent border-b-accent" to tab button when active
arb-tab-count          → "bg-accent/15 text-accent rounded-full px-2 py-0.5 text-xs font-bold"
arb-tab-content        → "flex flex-col gap-4"
arb-empty              → "text-slate-500 text-center py-8"
arb-card               → "border border-white/10 rounded-xl p-4"
arb-card--arb          → add "border-green-400/25" when arb card
arb-card--lay          → add "border-blue-400/25" when lay card
arb-card-header        → "flex items-center justify-between gap-3 mb-2 flex-wrap"
arb-card-time          → "text-slate-500 text-xs"
arb-card-title         → "m-0 mb-3 text-white text-base"
arb-badge              → "rounded-full px-2.5 py-0.5 text-xs font-bold"
arb-badge--profit      → "bg-green-400/15 text-green-400 border border-green-400/30"
arb-badge--lay         → "bg-blue-400/15 text-blue-400 border border-blue-400/30"
arb-badge--diff        → "bg-yellow-400/15 text-yellow-400 border border-yellow-400/30 inline-block"
arb-card-margin        → "flex items-center gap-2 mb-3 text-slate-400 text-sm"
arb-margin-bar         → "flex-1 h-1.5 rounded-full bg-white/10 overflow-hidden"
arb-margin-fill        → "block h-full bg-gradient-to-r from-green-400 via-yellow-400 to-red-400 rounded-full"
arb-table              → "w-full border-collapse text-sm"
arb-odds               → "text-accent font-bold"
arb-bet                → "text-green-400 font-semibold"
arb-lay-grid           → "grid grid-cols-[repeat(auto-fit,minmax(160px,1fr))] gap-2"
arb-lay-row            → "flex flex-col gap-0.5"
arb-lay-label          → "text-slate-500 text-xs uppercase tracking-wider font-semibold"
arb-lay-value          → "text-slate-200 text-base"
arb-modal-backdrop / arb-modal / arb-modal-header → replace InfoModal implementation with shared Modal

Mobile responsive (sm: prefixes):
  arb-page: add "sm:p-3" for smaller padding at mobile
  arb-title: add "sm:text-xl" for smaller font at mobile
  arb-header: add "sm:flex-wrap" for wrapping at mobile
  arb-table td: add "sm:px-1.5 sm:py-1.5" for tighter cell padding at mobile
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all arb-* CSS classes with Tailwind inline classes in arbitrage.jsx</name>
  <files>src/components/Projects/Arbitrage/arbitrage.jsx</files>
  <read_first>
    - src/components/Projects/Arbitrage/arbitrage.jsx (MUST read full file — 429-line CSS is the reference, read CSS too)
    - src/components/Projects/Arbitrage/arbitrage.css (read all class definitions to ensure every class is covered)
    - src/components/Shared/Modal.jsx (confirm Modal props interface)
    - .planning/phases/06-mini-projects/06-RESEARCH.md §Arbitrage (complete class mapping table)
  </read_first>
  <action>
Modify `arbitrage.jsx` — this is an existing functional component, so rewrite only what changes:

1. **Remove:** `import './arbitrage.css'`

2. **Add:** `import Modal from '../../Shared/Modal'` (for the InfoModal replacement)

3. **Preserve verbatim:** `import getArbitrageBets from './arbitrageCalcutations'` (typo in filename is intentional — do not correct)

4. **Replace each `className="arb-*"` value** using the exact mapping table in the interfaces block above. Every `arb-*` class name in the file must be replaced — use the RESEARCH.md class table as the authoritative source if the interfaces block is missing any class.

5. **Handle modifier classes** (arb-tab--active, arb-card--arb, arb-card--lay):
   - Active tab: compute className using template literal — `className={\`arb-tab base-classes \${isActive ? 'text-accent border-b-accent' : ''}\`}` pattern, removing the `arb-tab` and `arb-tab--active` strings.
   - Arb/lay card variants: same pattern — base card classes + variant modifier inline.

6. **InfoModal replacement:**
   - Find the `InfoModal` component (inline component or function inside arbitrage.jsx)
   - Replace its Bootstrap-style backdrop/modal divs with the shared `<Modal title={title} onClose={onClose}>{children}</Modal>` pattern
   - The `InfoModal` is called with `title` + `children` + `onClose` props — this interface matches the shared Modal exactly

7. **Root wrapper (arb-page):** `className="min-h-screen p-5 text-white sm:p-3"` — NO margin-top or mt-14 (D-12).

8. **All other component logic, state, API calls, and calculation logic:** Preserved verbatim. No logic changes.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File does NOT contain `import.*arbitrage.css`
    - File does NOT contain `className="arb-` (any remaining arb- class)
    - File does NOT contain `className="card"` (Bootstrap card class)
    - File contains `import getArbitrageBets from './arbitrageCalcutations'` (typo preserved)
    - File contains `import Modal from '../../Shared/Modal'`
    - Root div does NOT contain `mt-14` or `marginTop`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All arb-* classes replaced with Tailwind utilities, InfoModal uses shared Modal, calculation imports preserved, build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Delete arbitrage.css</name>
  <files>src/components/Projects/Arbitrage/arbitrage.css</files>
  <read_first>
    - src/components/Projects/Arbitrage/arbitrage.jsx (confirm import removed in Task 1)
  </read_first>
  <action>
Delete `src/components/Projects/Arbitrage/arbitrage.css`. All 429 lines of CSS have been replaced by Tailwind inline classes in Task 1. The import line has been removed. Per D-08 and D-15.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/Projects/Arbitrage/arbitrage.css` does NOT exist
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>arbitrage.css deleted, build passes with no dangling import errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → External odds API | getArbitrageBets() fetches from an external odds API (hardcoded API keys in arbitrageCalcutations.jsx) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Information Disclosure | Hardcoded API keys in arbitrageCalcutations.jsx | accept | Acknowledged in STATE.md as out of scope — tracked in fix/security branch. This plan makes no changes to arbitrageCalcutations.jsx. |
| T-06-05-02 | Spoofing | External odds API response | accept | Response is display-only — no server-side processing, no stored data. User sees what the API returns. Low risk. |
</threat_model>

<verification>
After both tasks:
- `npm run build` exits 0
- Navigate to `/Projects/Arbitrage` — page renders with Tailwind styling (dark background, gold title)
- Click "Fetch Bets" — loading state appears (spinner visible)
- Help/info modal opens and closes correctly using shared Modal
- No double top spacing
</verification>

<success_criteria>
All arb-* CSS classes replaced with inline Tailwind utilities, shared Modal used for InfoModal, arbitrage.css deleted, calculation logic untouched, and `npm run build` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-mini-projects/06-05-SUMMARY.md`
</output>
