---
phase: 08-topartistsmap-spotify
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified: []
autonomous: false
requirements:
  - SPOT-01
  - SPOT-02

must_haves:
  truths:
    - "The TopArtistsMap page renders correctly in the browser with Tailwind styling"
    - "The Spotify OAuth login flow initiates without errors when Load Top Artists is clicked"
    - "After authentication, the map displays with artist data and correct visual styling"
    - "The info modal opens, is dark-themed, and closes on backdrop click or the × button"
    - "The zoom controls work on desktop and switch to bottom-right vertical column on mobile viewports"
  artifacts:
    - path: "src/components/Projects/TopArtistsMap/topArtistsMap.jsx"
      provides: "Visually verified migrated component"
  key_links:
    - from: "topArtistsMap.jsx"
      to: "SpotifyAuthProvider.jsx"
      via: "SpotifyAuthProvider wrapper at export default"
      pattern: "SpotifyAuthProvider"
---

<objective>
Human visual and functional verification of the TopArtistsMap / Spotify migration. Confirms the UI looks correct with Tailwind styling, the Spotify OAuth flow initiates and completes, and the map renders with data after authentication.

Purpose: The auth flow requires a real browser session with Spotify credentials — this cannot be automated. Visual confirmation closes the phase.

Output: Human approval that SPOT-01 and SPOT-02 are satisfied, completing the final phase of the modernisation milestone.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-topartistsmap-spotify/08-01-SUMMARY.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual and functional verification of TopArtistsMap</name>

  <what-built>
    Plan 08-01 migrated topArtistsMap.jsx from custom CSS to Tailwind utility classes:
    - Outer wrapper: gold-bordered surface card, white text, no margin-top
    - Controls row: time range select (dark bg, accent border), "Load Top Artists" button (accent fill), "Log out" button (transparent/accent)
    - Loading spinner: Tailwind animate-spin (not custom @keyframes)
    - "How it works" button: white/slate theme
    - Info modal: replaced by shared dark-themed Modal.jsx (bg-surface, accent title)
    - Map wrapper: bg-white, rounded, border, conditional cursor-grab / cursor-grabbing
    - Zoom controls: top-right on desktop, bottom-right vertical column on mobile
    - Floating tooltip: hidden on mobile (max-md:hidden), orange-tinted border
    - Selected country tooltip: orange-tinted border, dark bg
    - Legend: color swatches with orange-tinted scale border
    - Unmapped artists: accent-tinted border, accent heading
    - topArtistsMap.css: deleted
  </what-built>

  <how-to-verify>
1. Start the dev server: `npm run dev` from the project root
2. Open http://127.0.0.1:5173 in the browser
3. Navigate to the TopArtistsMap page (Projects → Top Spotify Artists World Map)

**Visual checks (no auth needed):**
- Page shows the gold-bordered surface card with "Top Spotify Artists World Map" heading in accent (#ffc200)
- "Time range" select has dark background and gold border
- "Load Top Artists" button has gold fill with dark text
- "How it works" button has white background with slate text
- The map placeholder renders with a white background inside a rounded bordered container
- Zoom + / − / Reset buttons are visible in the top-right corner of the map
- The hint text ("Authenticate with Spotify...") is visible in muted white/80

**Info modal check:**
- Click "How it works"
- Confirm the modal opens with dark background (bg-surface, NOT the old white light modal)
- Confirm the title "How this map works" is in gold (#ffc200)
- Confirm the body text is readable (white/80)
- Confirm clicking outside the modal (backdrop) dismisses it
- Confirm the × button also dismisses it

**Mobile layout check (optional but recommended):**
- Open DevTools → responsive mode → set width to 375px
- Confirm zoom controls move to bottom-right and stack vertically
- Confirm the floating tooltip is hidden (no tooltip appears on hover)
- Confirm the "Pinch to zoom..." hint text appears below the map

**Auth flow check (SPOT-01 — requires Spotify credentials):**
- Click "Load Top Artists"
- Confirm a Spotify login page opens (PKCE OAuth redirect)
- Sign in with a Spotify account that has access (or observe the "development mode" hint if not approved)
- After auth, confirm the map renders with coloured countries and the legend appears
- Confirm hovering a coloured country shows the floating tooltip with artist names
- Confirm clicking a coloured country shows the tapped tooltip below the map
- Confirm "Log out" button appears and clicking it logs out cleanly

**Build check:**
- Run `npm run build` — confirm exit code 0, no errors
  </how-to-verify>

  <resume-signal>
    Type "approved" if all visual and functional checks pass.
    Or describe any issues found (e.g., "info modal still shows white background", "zoom buttons missing on mobile") so they can be fixed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Spotify OAuth | Human verifies the OAuth redirect initiates and completes — no automated trust boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Information Disclosure | Spotify OAuth session (verification step) | accept | Verification uses the developer's own Spotify credentials. No user data is at risk during the verification step. The auth flow security is handled by SpotifyAuthProvider.jsx (unchanged). |
</threat_model>

<verification>
This plan IS the verification step. Approval from the human verifier constitutes completion.

The phase is complete when the human types "approved" confirming:
1. Visual appearance matches the dark Tailwind theme
2. Info modal is dark-themed (not the old white modal)
3. Spotify OAuth login initiates correctly (SPOT-01)
4. Map renders with data after auth (SPOT-02)
5. `npm run build` passes
</verification>

<success_criteria>
- Human confirms the TopArtistsMap page renders with correct dark Tailwind styling
- Human confirms the info modal is dark-themed using Modal.jsx
- Human confirms the Spotify OAuth flow initiates (SPOT-01)
- Human confirms the map renders with data after authentication (SPOT-02)
- `npm run build` passes with exit code 0
</success_criteria>

<output>
After approval, create `.planning/phases/08-topartistsmap-spotify/08-02-SUMMARY.md` using the summary template.

This is the final plan of the final phase. After creating the summary, update `.planning/STATE.md` to mark Phase 8 complete and the milestone as finished.
</output>
