---
phase: 02-layout-navigation
plan: 04
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/components/SideDrawer/SideDrawer.jsx
  - src/components/Backdrop/Backdrop.jsx
  - src/components/SideDrawer/SideDrawer.css
  - src/components/Backdrop/Backdrop.css
autonomous: true
requirements:
  - NAV-02
  - NAV-03
  - NAV-04

must_haves:
  truths:
    - "SideDrawer.jsx uses NavLink from react-router-dom (no bare <a href> links)"
    - "SideDrawer.jsx accepts and uses a closeDrawer prop on each NavLink click"
    - "SideDrawer.jsx uses isActive from NavLink className prop to apply text-accent to the active route"
    - "SideDrawer.jsx image src is /BrendonWard.png (absolute path)"
    - "SideDrawer.jsx profile section is centered horizontally"
    - "SideDrawer.jsx has no CSS file import"
    - "Backdrop.jsx has no CSS file import"
    - "SideDrawer.css is deleted"
    - "Backdrop.css is deleted"
    - "The drawer slides in/out with a 0.3s ease-out transition"
  artifacts:
    - path: "src/components/SideDrawer/SideDrawer.jsx"
      provides: "SideDrawer with NavLink, Tailwind classes, closeDrawer prop, active link indicator"
      contains: "NavLink"
    - path: "src/components/Backdrop/Backdrop.jsx"
      provides: "Backdrop with Tailwind classes, no CSS import"
      contains: "bg-black/30"
  key_links:
    - from: "src/App.jsx"
      to: "src/components/SideDrawer/SideDrawer.jsx"
      via: "closeDrawer={backdropClickHandler} prop"
      pattern: "closeDrawer"
    - from: "src/App.jsx"
      to: "src/components/Backdrop/Backdrop.jsx"
      via: "click={backdropClickHandler} prop"
      pattern: "<Backdrop"
---

<objective>
Rewrite SideDrawer.jsx with NavLink (replacing bare `<a href>` links), Tailwind CSS replacing SideDrawer.css, a closeDrawer prop for close-on-navigate behavior, and a centered profile section with fixed image path. Rewrite Backdrop.jsx with Tailwind. Delete both CSS files.

Purpose: NAV-02 (hamburger open/close), NAV-03 (NavLink replaces `<a href>`), and NAV-04 (active route indicator) are all delivered in this plan. The drawer slide animation is replicated in Tailwind using translate-x classes and transition utilities. The closeDrawer prop (passed from App.jsx in Plan 02) enables close-on-navigate — each NavLink click sets sideDrawerOpen to false. NavLink's className prop as a function provides the isActive hook for the gold accent active state.

Output: SideDrawer.jsx uses NavLink throughout, Tailwind for all styling, and properly handles open/close state and active route highlighting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Git/Portfolio-Website-2024/.planning/PROJECT.md
@C:/Git/Portfolio-Website-2024/.planning/ROADMAP.md
@C:/Git/Portfolio-Website-2024/.planning/STATE.md
@C:/Git/Portfolio-Website-2024/.planning/phases/02-layout-navigation/02-CONTEXT.md
@C:/Git/Portfolio-Website-2024/.planning/phases/02-layout-navigation/02-01-SUMMARY.md

<interfaces>
<!-- Current state of files being modified -->

src/components/SideDrawer/SideDrawer.jsx — current full content:
```jsx
import React from 'react';
import './SideDrawer.css';

const SideDrawer = props => {
    let drawerClass = 'side-drawer';
    if(props.show) {
        drawerClass = 'side-drawer open';
    }
    
    return (
    <nav className={drawerClass} >
        <ul>
            <li>
                <a href="/" className="picture-wrapper">
                    <img src='../BrendonWard.png' alt="Brendon Ward" className="side-drawer-picture"/>
                    <div className="side-drawer-picture-caption">Brendon Ward</div>
                </a>
            </li>
            <li><a href="/About" className="drawer-item">About Me</a></li>
            <li><a href="/CV" className="drawer-item">CV</a></li>
            <li><a href="/Projects" className="drawer-item">Personal Projects</a></li>
            <li><a href="/Photos" className="drawer-item">Photos and Videos</a></li>
            <li><a href="/Music" className="drawer-item">Music</a></li>
            <li><a href="/BusinessBrains" className="drawer-item">Business Brains</a></li>
        </ul>
    </nav>)
};

export default SideDrawer;
```

src/components/SideDrawer/SideDrawer.css — current full content (to be deleted):
```css
.side-drawer {
    height: 100%;
    background: #333134;
    box-shadow: 1px 0px 7px rgba(0,0,0,0.5);
    position: fixed;
    top: 0;
    left: 0;
    width: 70%;
    max-width: 400px;
    z-index: 200;
    transform: translateX(-100%);
    transition: transform 0.3s ease-out;
}
.side-drawer.open {
    transform: translateX(0)
}
```

src/components/Backdrop/Backdrop.jsx — current full content:
```jsx
import React from 'react';
import './Backdrop.css';

const Backdrop = props => (
    <div className="backdrop" onClick={props.click}/>
);

export default Backdrop;
```

src/components/Backdrop/Backdrop.css — current full content (to be deleted):
```css
.backdrop {
    position: fixed;
    width: 100%;
    height: 100%;
    background: rgba(0,0,0,0.3);
    z-index: 100;
    top: 0;
    left: 0;
}
```

CSS measurement mapping (from original CSS to Tailwind):
- Drawer background: #333134 → `bg-surface` (--color-surface token)
- Drawer box-shadow: 1px 0px 7px rgba(0,0,0,0.5) → `shadow-[1px_0px_7px_rgba(0,0,0,0.5)]` (arbitrary shadow)
- Drawer z-index: 200 → `z-[200]`
- Drawer width: 70% → `w-[70%]`
- Drawer max-width: 400px → `max-w-[400px]`
- Drawer translateX(-100%) → `-translate-x-full`
- Drawer translateX(0) → `translate-x-0`
- Drawer transition: transform 0.3s ease-out → `transition-transform duration-300 ease-out`
- Backdrop background: rgba(0,0,0,0.3) → `bg-black/30`
- Backdrop z-index: 100 → `z-[100]`

NavLink pattern (D-10):
```jsx
className={({ isActive }) => `block text-xl no-underline hover:text-accent ${isActive ? 'text-accent' : 'text-white'}`}
```

Cross-plan interface note: App.jsx (Plan 02) passes `closeDrawer={backdropClickHandler}` to SideDrawer. This plan adds `closeDrawer` as a prop and calls `onClick={closeDrawer}` on every NavLink. The two plans are in the same wave and touch different files — no conflict.

Image path fix (D-16):
- Current: `src='../BrendonWard.png'` — relative path that breaks because Vite serves from the project root, not relative to the component
- Target: `src='/BrendonWard.png'` — absolute path from the public/ directory (correct Vite convention)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SideDrawer.jsx with NavLink, Tailwind, closeDrawer prop, and centered profile</name>
  <files>src/components/SideDrawer/SideDrawer.jsx, src/components/SideDrawer/SideDrawer.css</files>
  <read_first>
    - C:/Git/Portfolio-Website-2024/src/components/SideDrawer/SideDrawer.jsx — current state shown in interfaces block above; read before editing
    - C:/Git/Portfolio-Website-2024/src/components/SideDrawer/SideDrawer.css — current state shown in interfaces block above; read before deleting
    - C:/Git/Portfolio-Website-2024/src/index.css — confirm bg-surface, text-accent, text-white tokens are defined in @theme
    - C:/Git/Portfolio-Website-2024/public/BrendonWard.png — confirm the image file exists at this path
  </read_first>
  <action>
**Edit 1: Rewrite SideDrawer.jsx**

Replace the ENTIRE content of src/components/SideDrawer/SideDrawer.jsx with the following. Every line matters.

```jsx
import React from 'react';
import { NavLink } from 'react-router-dom';

const navLinks = [
  { to: '/About', label: 'About Me' },
  { to: '/CV', label: 'CV' },
  { to: '/Projects', label: 'Personal Projects' },
  { to: '/Photos', label: 'Photos and Videos' },
  { to: '/Music', label: 'Music' },
  { to: '/BusinessBrains', label: 'Business Brains' },
];

const SideDrawer = ({ show, closeDrawer }) => (
  <nav
    className={`fixed top-0 left-0 h-full w-[70%] max-w-[400px] bg-surface shadow-[1px_0px_7px_rgba(0,0,0,0.5)] z-[200] transition-transform duration-300 ease-out ${show ? 'translate-x-0' : '-translate-x-full'}`}
  >
    <ul className="list-none p-0 m-0 flex flex-col h-full">
      <li className="flex justify-center py-6 px-4">
        <NavLink to="/" onClick={closeDrawer} className="flex flex-col items-center gap-3 no-underline">
          <img src="/BrendonWard.png" alt="Brendon Ward" className="w-24 h-auto" />
          <span className="text-white text-base font-medium">Brendon Ward</span>
        </NavLink>
      </li>
      {navLinks.map(({ to, label }) => (
        <li key={to} className="my-1 px-4">
          <NavLink
            to={to}
            onClick={closeDrawer}
            className={({ isActive }) =>
              `block text-xl no-underline hover:text-accent ${isActive ? 'text-accent' : 'text-white'}`
            }
          >
            {label}
          </NavLink>
        </li>
      ))}
    </ul>
  </nav>
);

export default SideDrawer;
```

Key decisions applied:
- D-07: `<NavLink to={to}>` replaces `<a href={to}>` throughout — React Router navigation, no full page reloads
- D-08: No `exact` prop on NavLinks (valid in react-router-dom v7 — exact matching is default behavior in v7 Routes)
- D-09: `onClick={closeDrawer}` on every NavLink — calls `setSideDrawerOpen(false)` in App.jsx on navigation
- D-10/D-11: NavLink className as function `({ isActive }) => ... isActive ? 'text-accent' : 'text-white'` — gold accent for active, white for inactive (SideDrawer only per D-12)
- D-13/D-14: Drawer closes on nav click (closeDrawer) AND on backdrop click (handled in App.jsx)
- D-15: Profile section kept at the top of the drawer
- D-16: Image src changed from `../BrendonWard.png` to `/BrendonWard.png` (absolute Vite public path)
- D-17: No `rounded-full` applied to the image — it's already circular via transparent PNG background
- D-18: Profile section centered with `flex justify-center` on the `<li>`, `flex flex-col items-center` on the NavLink
- Drawer slide: `translate-x-0` when open, `-translate-x-full` when closed, with `transition-transform duration-300 ease-out`
- Drawer styling: `bg-surface` (#333134), `shadow-[1px_0px_7px_rgba(0,0,0,0.5)]`, `z-[200]`, `w-[70%] max-w-[400px]`

**Edit 2: Delete SideDrawer.css**

Delete the file `src/components/SideDrawer/SideDrawer.css`.

```bash
rm src/components/SideDrawer/SideDrawer.css
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('src/components/SideDrawer/SideDrawer.jsx','utf8');const checks={'no-css-import':!c.includes(\"import './SideDrawer.css'\"),'has-NavLink':c.includes('NavLink'),'no-bare-a-href':!c.includes('<a href'),'has-closeDrawer':c.includes('closeDrawer'),'has-isActive':c.includes('isActive'),'has-text-accent':c.includes('text-accent'),'has-abs-image-path':c.includes('\"/BrendonWard.png\"'),'no-rel-image-path':!c.includes('../BrendonWard.png'),'has-bg-surface':c.includes('bg-surface'),'has-translate-full':c.includes('-translate-x-full'),'has-translate-0':c.includes('translate-x-0'),'has-transition':c.includes('transition-transform'),'css-deleted':!fs.existsSync('src/components/SideDrawer/SideDrawer.css')};let fail=false;Object.entries(checks).forEach(([k,v])=>{if(!v){console.error('FAIL:',k);fail=true}});if(!fail)console.log('PASS: SideDrawer migrated correctly')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/SideDrawer/SideDrawer.jsx` does NOT contain `import './SideDrawer.css'`
    - `src/components/SideDrawer/SideDrawer.jsx` contains `NavLink` (imported and used)
    - `src/components/SideDrawer/SideDrawer.jsx` does NOT contain `<a href` (bare anchor tags fully removed)
    - `src/components/SideDrawer/SideDrawer.jsx` contains `closeDrawer` (prop accepted and called on every NavLink)
    - `src/components/SideDrawer/SideDrawer.jsx` contains `isActive` (NavLink className function for active state)
    - `src/components/SideDrawer/SideDrawer.jsx` contains `text-accent` (active link gold colour, D-11)
    - `src/components/SideDrawer/SideDrawer.jsx` contains `"/BrendonWard.png"` (absolute image path, D-16)
    - `src/components/SideDrawer/SideDrawer.jsx` does NOT contain `../BrendonWard.png` (relative path removed)
    - `src/components/SideDrawer/SideDrawer.jsx` contains `bg-surface` (drawer background colour)
    - `src/components/SideDrawer/SideDrawer.jsx` contains `-translate-x-full` (closed state)
    - `src/components/SideDrawer/SideDrawer.jsx` contains `translate-x-0` (open state)
    - `src/components/SideDrawer/SideDrawer.jsx` contains `transition-transform` (slide animation)
    - `src/components/SideDrawer/SideDrawer.css` does NOT exist on disk
    - The verify node command exits 0 and prints "PASS: SideDrawer migrated correctly"
  </acceptance_criteria>
  <done>SideDrawer.jsx uses NavLink throughout, Tailwind utilities for all styling, closeDrawer prop for close-on-navigate, isActive for gold active indicator, centered profile section, fixed image path. SideDrawer.css deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Backdrop.jsx with Tailwind, delete Backdrop.css; run full Wave 2 build gate</name>
  <files>src/components/Backdrop/Backdrop.jsx, src/components/Backdrop/Backdrop.css</files>
  <read_first>
    - C:/Git/Portfolio-Website-2024/src/components/Backdrop/Backdrop.jsx — current state shown in interfaces block above; read before editing
    - C:/Git/Portfolio-Website-2024/src/components/Backdrop/Backdrop.css — current state shown in interfaces block above; read before deleting
  </read_first>
  <action>
**Edit 1: Rewrite Backdrop.jsx**

Replace the ENTIRE content of src/components/Backdrop/Backdrop.jsx with:

```jsx
import React from 'react';

const Backdrop = ({ click }) => (
  <div
    className="fixed top-0 left-0 w-full h-full bg-black/30 z-[100]"
    onClick={click}
  />
);

export default Backdrop;
```

Changes from original:
- Remove `import './Backdrop.css'`
- Replace `className="backdrop"` with Tailwind utilities:
  - `fixed top-0 left-0` → position:fixed, top:0, left:0
  - `w-full h-full` → width:100%, height:100%
  - `bg-black/30` → background:rgba(0,0,0,0.3) — black at 30% opacity (Tailwind opacity modifier)
  - `z-[100]` → z-index:100 (arbitrary value; above toolbar z-[1] but below drawer z-[200])
- Use destructured props `{ click }` instead of `props`

**Edit 2: Delete Backdrop.css**

Delete the file `src/components/Backdrop/Backdrop.css`.

```bash
rm src/components/Backdrop/Backdrop.css
```

**Build gate (after Backdrop rewrite, confirms all Wave 2 plans work together):**

After deleting Backdrop.css, run the production build:

```bash
npm run build
```

This must exit 0. A successful build confirms:
- All Wave 2 component rewrites compile without errors
- Tailwind processes all new utility classes correctly
- BrowserRouter is correctly placed at App.jsx level (Plan 02)
- No CSS import errors from deleted CSS files
- NavLink imports resolve correctly in SideDrawer.jsx
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('src/components/Backdrop/Backdrop.jsx','utf8');const checks={'no-css-import':!c.includes(\"import './Backdrop.css'\"),'has-bg-black-30':c.includes('bg-black/30'),'has-z-100':c.includes('z-[100]'),'has-fixed':c.includes('fixed'),'has-w-full':c.includes('w-full'),'has-h-full':c.includes('h-full'),'css-deleted':!fs.existsSync('src/components/Backdrop/Backdrop.css')};let fail=false;Object.entries(checks).forEach(([k,v])=>{if(!v){console.error('FAIL:',k);fail=true}});if(!fail)console.log('PASS: Backdrop migrated correctly')" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Backdrop/Backdrop.jsx` does NOT contain `import './Backdrop.css'`
    - `src/components/Backdrop/Backdrop.jsx` contains `bg-black/30` (rgba(0,0,0,0.3) backdrop)
    - `src/components/Backdrop/Backdrop.jsx` contains `z-[100]` (z-index: 100 — above toolbar, below drawer)
    - `src/components/Backdrop/Backdrop.jsx` contains `fixed` (fixed positioning)
    - `src/components/Backdrop/Backdrop.jsx` contains `w-full` and `h-full` (full viewport)
    - `src/components/Backdrop/Backdrop.css` does NOT exist on disk
    - `npm run build` exits with code 0
    - A `dist/` directory is created or updated with built assets
    - The verify node command prints "PASS: Backdrop migrated correctly" and npm build exits 0
  </acceptance_criteria>
  <done>Backdrop.jsx uses Tailwind utilities. Backdrop.css deleted. npm run build exits 0, confirming all Wave 2 plans (02, 03, 04) integrate correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React Router context | NavLink must be inside a BrowserRouter (provided by App.jsx from Plan 02). If Plan 04 runs before Plan 02 restores the router context, NavLinks would throw at runtime. The wave 2 dependency on Plan 01 and the fact that App.jsx is the router provider (Plan 02 runs in the same wave) ensures correctness. |
| User-controlled navigation | NavLink `to` props use static string literals — not constructed from user input. No injection risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | NavLink outside Router context | mitigate | NavLink requires BrowserRouter ancestor. App.jsx (Plan 02) provides BrowserRouter in the same wave. The `depends_on: ["02-01"]` ensures Plan 01 has already cleared the old BrowserRouter from main.jsx. Both plans in wave 2 are required to complete before the app is functional. The build gate (npm run build) at the end of Plan 04 catches any missing context errors. |
| T-04-02 | Tampering | Bare `<a href>` removal | mitigate | Each bare `<a href="/X">` causes a full page reload (bypassing React Router). Replacing with `<NavLink to="/X">` routes through React Router. Acceptance criteria explicitly checks `!c.includes('<a href')` to confirm no bare anchors remain. |
| T-04-03 | Information Disclosure | Image path change | accept | Changing `../BrendonWard.png` to `/BrendonWard.png` is a path fix, not a security change. The image is in public/ and is intentionally publicly served. The absolute path is the correct Vite convention for public assets. |
</threat_model>

<verification>
After both tasks complete, run the full Wave 2 verification:

```bash
node -e "
const fs=require('fs');
const app=fs.readFileSync('src/App.jsx','utf8');
const main=fs.readFileSync('src/components/main.jsx','utf8');
const drawer=fs.readFileSync('src/components/SideDrawer/SideDrawer.jsx','utf8');
const toolbar=fs.readFileSync('src/components/Toolbar/Toolbar.jsx','utf8');
const backdrop=fs.readFileSync('src/components/Backdrop/Backdrop.jsx','utf8');
const toggle=fs.readFileSync('src/components/SideDrawer/DrawerToggleButton.jsx','utf8');
const checks={
  'app-functional':!app.includes('class App')&&app.includes('useState'),
  'app-browserrouter':app.includes('BrowserRouter'),
  'app-closedrawer':app.includes('closeDrawer'),
  'main-no-browserrouter':!main.includes('BrowserRouter'),
  'main-no-exact':!main.includes('exact'),
  'drawer-navlink':drawer.includes('NavLink')&&!drawer.includes('<a href'),
  'drawer-isactive':drawer.includes('isActive'),
  'drawer-abs-image':drawer.includes('/BrendonWard.png'),
  'toolbar-h14':toolbar.includes('h-14')&&toolbar.includes('fixed'),
  'toggle-3-lines':(toggle.match(/h-\[3px\]/g)||[]).length===3,
  'backdrop-bg-black-30':backdrop.includes('bg-black/30'),
  'app-css-gone':!fs.existsSync('src/App.css'),
  'toolbar-css-gone':!fs.existsSync('src/components/Toolbar/Toolbar.css'),
  'toggle-css-gone':!fs.existsSync('src/components/SideDrawer/DrawerToggleButton.css'),
  'drawer-css-gone':!fs.existsSync('src/components/SideDrawer/SideDrawer.css'),
  'backdrop-css-gone':!fs.existsSync('src/components/Backdrop/Backdrop.css')
};
let fail=false;
Object.entries(checks).forEach(([k,v])=>{if(!v){console.error('FAIL:',k);fail=true}});
if(!fail)console.log('ALL PHASE 2 CHECKS PASSED')
"
```

Then run the build gate:
```bash
npm run build
```

Both must succeed.

Manual verification (dev server):
1. Run `npm run dev`
2. Open http://127.0.0.1:5173
3. Click the hamburger button — drawer should slide in from the left
4. Click a nav link (e.g., /About) — drawer should close and navigate (no full page reload)
5. The active route link should show in gold (#ffc200) in the SideDrawer
6. Click the backdrop (dark overlay) — drawer should close
7. Verify profile photo appears centered at the top of the drawer
</verification>

<success_criteria>
- SideDrawer uses NavLink (NAV-03: no bare `<a href>` links)
- Hamburger opens/closes the drawer with slide animation (NAV-02)
- Active route shows gold accent colour in SideDrawer (NAV-04)
- closeDrawer prop closes drawer on nav link click (D-09)
- Profile section centered with absolute image path (D-15, D-16, D-17, D-18)
- SideDrawer.css and Backdrop.css deleted (D-19)
- npm run build exits 0 (full Wave 2 integration check)
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-navigation/02-04-SUMMARY.md` following the summary template.
</output>
