---
phase: 02-layout-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/main.jsx
autonomous: true
requirements:
  - QUAL-02

must_haves:
  truths:
    - "main.jsx does not contain <BrowserRouter> or import BrowserRouter"
    - "all 16 <Route> elements in main.jsx have no exact prop"
    - "npm run build exits 0 after this plan"
  artifacts:
    - path: "src/components/main.jsx"
      provides: "Route definitions without BrowserRouter wrapper and without exact props"
      contains: "<Routes>"
  key_links:
    - from: "src/App.jsx"
      to: "src/components/main.jsx"
      via: "import Main from './components/main'"
      pattern: "<Main"
---

<objective>
Remove BrowserRouter from main.jsx and strip the invalid `exact` prop from all Route elements.

Purpose: App.jsx (Plan 02) will wrap the entire layout in a single BrowserRouter. Having BrowserRouter in both main.jsx and App.jsx simultaneously would throw a React error ("You cannot render a <Router> inside another <Router>"). This plan clears the BrowserRouter out of main.jsx first so Plan 02 can safely add it to App.jsx without a conflict. The `exact` prop is also invalid in react-router-dom v7 (QUAL-02) — removing it here keeps the codebase clean before component rewrites begin.

Output: main.jsx re-exports only a `<Routes>` tree with `<Route>` elements and no `exact` prop anywhere.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Git/Portfolio-Website-2024/.planning/PROJECT.md
@C:/Git/Portfolio-Website-2024/.planning/ROADMAP.md
@C:/Git/Portfolio-Website-2024/.planning/STATE.md
@C:/Git/Portfolio-Website-2024/.planning/phases/02-layout-navigation/02-CONTEXT.md

<interfaces>
<!-- Current state of main.jsx before this edit -->

src/components/main.jsx — current full content:
```jsx
import React from 'react';
import {BrowserRouter, Routes, Route} from 'react-router-dom';
import Landing from './Landing/landingpage';
import About from './About/about';
import CV from './CV/cv';
import Projects from './Projects/projects';
import TrainGame from './Projects/TrainGame/trainGame';
import PaceCalculator from './Projects/PaceCalculator/paceCalculator';
import FiveHundred from './Projects/500/500.jsx';
import Photos from './Photos/photos';
import NoisyDetector from './Projects/NoisyDetector/noisyDetector';
import Arbitrage from './Projects/Arbitrage/arbitrage';
import TrioHome from './Projects/Trio/trioHome';
import TrioGame from './Projects/Trio/trioGame';
import Music from './Music/music';
import BusinessBrains from './BusinessBrains/businessBrains';
import NBALadder from './Projects/NBA-Ladder/NBA-Ladder';
import TopArtistsMap from './Projects/TopArtistsMap/topArtistsMap';


const Main = () => (
    <BrowserRouter>
        <Routes>
            <Route path="/" exact element={<Landing/>}/>
            <Route path="/About" exact element={<About/>}/>
            <Route path="/CV" exact element={<CV/>}/>
            <Route path="/Projects" exact element={<Projects/>}/>
            <Route path="/Projects/Arbitrage" exact element={<Arbitrage/>}/>
            <Route path="/Projects/Trio" exact element={<TrioHome/>}/>
            <Route path="/Projects/TrioGame" exact element={<TrioGame/>}/>
            <Route path="/Projects/TrainGame" exact element={<TrainGame/>}/>
            <Route path="/Projects/PaceCalculator" exact element={<PaceCalculator/>}/>
            <Route path="/Projects/FiveHundred" exact element={<FiveHundred/>}/>
            <Route path="/Projects/Noise" exact element={<NoisyDetector/>}/>
            <Route path="/Projects/NBA-Ladder" exact element={<NBALadder/>}/>
            <Route path="/Projects/TopArtistsMap" exact element={<TopArtistsMap/>}/>
            <Route path="/Photos" exact element={<Photos/>}/>
            <Route path="/Music" exact element={<Music/>}/>
            <Route path="/BusinessBrains" exact element={<BusinessBrains/>}/>
        </Routes>
    </BrowserRouter>
)

export default Main;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove BrowserRouter from main.jsx and strip all exact props</name>
  <files>src/components/main.jsx</files>
  <read_first>
    - C:/Git/Portfolio-Website-2024/src/components/main.jsx — current state is shown in the interfaces block above; read before editing to confirm it still matches
  </read_first>
  <action>
Replace the ENTIRE content of src/components/main.jsx with the following. Two changes are made:

1. Import line: remove `BrowserRouter` from the react-router-dom import (keep `Routes` and `Route`)
2. JSX: remove the `<BrowserRouter>` wrapper and its closing `</BrowserRouter>` tag (keep `<Routes>` as the root)
3. Each `<Route>`: remove the `exact` prop from all 16 routes (leave all other props — `path` and `element` — unchanged)

Complete target file content (write verbatim):
```jsx
import React from 'react';
import {Routes, Route} from 'react-router-dom';
import Landing from './Landing/landingpage';
import About from './About/about';
import CV from './CV/cv';
import Projects from './Projects/projects';
import TrainGame from './Projects/TrainGame/trainGame';
import PaceCalculator from './Projects/PaceCalculator/paceCalculator';
import FiveHundred from './Projects/500/500.jsx';
import Photos from './Photos/photos';
import NoisyDetector from './Projects/NoisyDetector/noisyDetector';
import Arbitrage from './Projects/Arbitrage/arbitrage';
import TrioHome from './Projects/Trio/trioHome';
import TrioGame from './Projects/Trio/trioGame';
import Music from './Music/music';
import BusinessBrains from './BusinessBrains/businessBrains';
import NBALadder from './Projects/NBA-Ladder/NBA-Ladder';
import TopArtistsMap from './Projects/TopArtistsMap/topArtistsMap';


const Main = () => (
    <Routes>
        <Route path="/" element={<Landing/>}/>
        <Route path="/About" element={<About/>}/>
        <Route path="/CV" element={<CV/>}/>
        <Route path="/Projects" element={<Projects/>}/>
        <Route path="/Projects/Arbitrage" element={<Arbitrage/>}/>
        <Route path="/Projects/Trio" element={<TrioHome/>}/>
        <Route path="/Projects/TrioGame" element={<TrioGame/>}/>
        <Route path="/Projects/TrainGame" element={<TrainGame/>}/>
        <Route path="/Projects/PaceCalculator" element={<PaceCalculator/>}/>
        <Route path="/Projects/FiveHundred" element={<FiveHundred/>}/>
        <Route path="/Projects/Noise" element={<NoisyDetector/>}/>
        <Route path="/Projects/NBA-Ladder" element={<NBALadder/>}/>
        <Route path="/Projects/TopArtistsMap" element={<TopArtistsMap/>}/>
        <Route path="/Photos" element={<Photos/>}/>
        <Route path="/Music" element={<Music/>}/>
        <Route path="/BusinessBrains" element={<BusinessBrains/>}/>
    </Routes>
)

export default Main;
```

IMPORTANT: After this edit, the app is temporarily broken until Plan 02 adds BrowserRouter to App.jsx. Do NOT run the dev server between Plan 01 and Plan 02 — the app will throw "useRoutes() may be used only in the context of a <Router> component". This is expected and transient.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('src/components/main.jsx','utf8');if(c.includes('BrowserRouter')){console.error('FAIL: BrowserRouter still present');process.exit(1)}if(c.includes('exact')){console.error('FAIL: exact prop still present');process.exit(1)}if(!c.includes('<Routes>')){console.error('FAIL: Routes element missing');process.exit(1)}if((c.match(/<Route /g)||[]).length!==16){console.error('FAIL: expected 16 Route elements, got '+(c.match(/<Route /g)||[]).length);process.exit(1)}console.log('PASS: main.jsx updated correctly')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/main.jsx` does NOT contain `BrowserRouter` anywhere (import or JSX)
    - `src/components/main.jsx` does NOT contain the string `exact` anywhere
    - `src/components/main.jsx` contains `<Routes>` as the root JSX element
    - `src/components/main.jsx` contains exactly 16 `<Route ` elements (all original routes preserved)
    - All 16 routes retain their `path` and `element` props unchanged
    - The verify node command exits 0 and prints "PASS: main.jsx updated correctly"
  </acceptance_criteria>
  <done>main.jsx exports a Routes tree without BrowserRouter wrapper and without any exact props. BrowserRouter will be added to App.jsx in Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React Router context | BrowserRouter provides the routing context; removing it here creates a transient broken state between Plan 01 and Plan 02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | Transient router absence | accept | Between Plan 01 (remove BrowserRouter from main.jsx) and Plan 02 (add to App.jsx), the app throws a router context error. This is expected and transient — both plans are in the same execution run and the dev server is not started mid-phase. Acceptance criteria for Plan 02 includes a build check that confirms the app compiles cleanly after the router is restored. |
| T-01-02 | Tampering | Route path matching | accept | Removing `exact` prop changes route matching semantics from v5 to v7. In react-router-dom v7, `<Routes>` uses exact matching by default — `exact` is not only invalid but was already a no-op once the codebase moved to v7 Routes. No routes are lost or mis-matched by this removal. |
</threat_model>

<verification>
After Task 1 completes, run the verification check:

```bash
node -e "const fs=require('fs');const c=fs.readFileSync('src/components/main.jsx','utf8');if(c.includes('BrowserRouter')){console.error('FAIL: BrowserRouter still present');process.exit(1)}if(c.includes('exact')){console.error('FAIL: exact prop still present');process.exit(1)}if(!c.includes('<Routes>')){console.error('FAIL: Routes element missing');process.exit(1)}if((c.match(/<Route /g)||[]).length!==16){console.error('FAIL: expected 16 Route elements, got '+(c.match(/<Route /g)||[]).length);process.exit(1)}console.log('PASS: main.jsx updated correctly')"
```

Must print "PASS: main.jsx updated correctly" and exit 0.

Note: Do NOT run `npm run build` after this plan alone — the build will fail because App.jsx still imports and renders Main without a wrapping BrowserRouter. The build gate runs after Plan 02 completes and the full router context is restored.
</verification>

<success_criteria>
- BrowserRouter removed from main.jsx import and JSX (D-02 prerequisite)
- All 16 `<Route>` elements have no `exact` prop (QUAL-02)
- All 16 original route paths and elements are preserved unchanged
- No postcondition regressions: all route paths still present
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-navigation/02-01-SUMMARY.md` following the summary template.
</output>
