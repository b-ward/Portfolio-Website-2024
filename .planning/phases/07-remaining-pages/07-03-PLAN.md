---
phase: 07-remaining-pages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/BusinessBrains/businessBrains.jsx
  - src/components/BusinessBrains/businessBrains.css
autonomous: true
requirements:
  - PAGE-03

must_haves:
  truths:
    - "The BusinessBrains page renders its two Spotify iframes inside an 80%-wide centred wrapper"
    - "The page title reads 'Business Brains' in gold (#ffc200) and is centred"
    - "Each iframe has a 2rem bottom margin via mb-8"
    - "No Bootstrap or custom CSS classes exist in the rendered component"
    - "businessBrains.css no longer exists in the repository"
  artifacts:
    - path: "src/components/BusinessBrains/businessBrains.jsx"
      provides: "Functional BusinessBrains component with Tailwind classes"
      contains: "const BusinessBrains"
    - path: "src/components/BusinessBrains/businessBrains.css"
      provides: "CSS file must be DELETED"
  key_links:
    - from: "src/components/BusinessBrains/businessBrains.jsx"
      to: "src/index.css @theme tokens"
      via: "text-accent Tailwind utility class"
      pattern: "text-accent"
---

<objective>
Convert businessBrains.jsx from a class component to a functional component with Tailwind CSS, migrating the business-brains-wrapper and business-brains-audio custom CSS classes to Tailwind equivalents, and delete businessBrains.css.

Purpose: Completes the Tailwind migration for the BusinessBrains page — the final component in Phase 7.
Output: Functional businessBrains.jsx with Tailwind classes, businessBrains.css deleted.
</objective>

<execution_context>
@C:/Users/bward/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bward/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-remaining-pages/07-CONTEXT.md
@.planning/phases/07-remaining-pages/07-PATTERNS.md

<interfaces>
<!-- Tailwind theme tokens from src/index.css -->
<!-- text-accent = #ffc200, bg-bg = #242424, bg-surface = #333134 -->
<!-- pt-14 is already on <main> in App.jsx — no top margin needed on component wrappers -->

<!-- Wrapper pattern (D-03): w-4/5 mx-auto mt-8 mb-8 — identical to Music -->
<!-- Per-iframe pattern (D-04): mb-8 class on each <iframe>; width="100%" height="232" stay as HTML attributes -->
<!-- Title pattern (D-05): text-accent text-center text-2xl font-semibold mb-4 -->
<!-- allowtransparency in source is lowercase — React requires camelCase allowTransparency -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite businessBrains.jsx as functional Tailwind component and delete businessBrains.css</name>
  <files>src/components/BusinessBrains/businessBrains.jsx, src/components/BusinessBrains/businessBrains.css</files>
  <read_first>
    - src/components/BusinessBrains/businessBrains.jsx — current class component to be replaced; note lowercase `allowtransparency` that must become camelCase `allowTransparency` in JSX
    - src/components/BusinessBrains/businessBrains.css — CSS file to confirm it exists before deletion
    - .planning/phases/07-remaining-pages/07-CONTEXT.md — D-01, D-03, D-04, D-05, D-07 decisions
    - .planning/phases/07-remaining-pages/07-PATTERNS.md — BusinessBrains section with exact Tailwind class strings and allowTransparency note
  </read_first>
  <action>
Overwrite src/components/BusinessBrains/businessBrains.jsx with the following content exactly (per D-01 functional conversion, D-03 wrapper, D-04 iframe spacing, D-05 title, D-07 CSS deletion):

Note: The source file has `allowtransparency="true"` (lowercase HTML attribute). React requires camelCase: `allowTransparency={true}`. The new file uses the correct JSX form.

```jsx
import React from 'react';

const BusinessBrains = () => (
  <div>
    <h3 className="text-accent text-center text-2xl font-semibold mb-4">Business Brains</h3>
    <div className="w-4/5 mx-auto mt-8 mb-8">
      <iframe
        className="mb-8"
        title="Pod 2"
        src="https://open.spotify.com/embed/episode/3uqsIqXuVVHPeMTlB5FQ1i?theme=0"
        width="100%"
        height="232"
        frameBorder="0"
        allowTransparency={true}
        allow="encrypted-media"
      ></iframe>
      <iframe
        className="mb-8"
        title="Pod 1"
        src="https://open.spotify.com/embed/episode/0eoHeo60heCHlVDOHZOug7?theme=0"
        width="100%"
        height="232"
        frameBorder="0"
        allowTransparency={true}
        allow="encrypted-media"
      ></iframe>
    </div>
  </div>
);

export default BusinessBrains;
```

Then delete src/components/BusinessBrains/businessBrains.css.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('src/components/BusinessBrains/businessBrains.jsx','utf8'); if(c.includes('class BusinessBrains')) throw new Error('class component not removed'); if(c.includes('businessBrains.css')) throw new Error('CSS import not removed'); if(!c.includes('const BusinessBrains')) throw new Error('functional component missing'); if(!c.includes('text-accent')) throw new Error('text-accent missing'); if(!c.includes('w-4/5 mx-auto mt-8 mb-8')) throw new Error('wrapper classes missing'); if(c.includes('business-brains-wrapper')) throw new Error('old CSS class still present'); if(c.includes('business-brains-audio')) throw new Error('old CSS class still present'); if(c.includes('allowtransparency=')) throw new Error('lowercase allowtransparency not fixed to camelCase'); console.log('PASS');"</automated>
    Check businessBrains.css was deleted: `node -e "const fs=require('fs'); if(fs.existsSync('src/components/BusinessBrains/businessBrains.css')) throw new Error('businessBrains.css still exists'); console.log('PASS');"`
    Check build: `npm run build` exits 0
  </verify>
  <acceptance_criteria>
    - src/components/BusinessBrains/businessBrains.jsx contains `const BusinessBrains` (functional component)
    - src/components/BusinessBrains/businessBrains.jsx does NOT contain `class BusinessBrains`
    - src/components/BusinessBrains/businessBrains.jsx does NOT contain `import './businessBrains.css'`
    - src/components/BusinessBrains/businessBrains.jsx does NOT contain `business-brains-wrapper` or `business-brains-audio` as className values
    - src/components/BusinessBrains/businessBrains.jsx contains `text-accent text-center text-2xl font-semibold mb-4` on the h3
    - src/components/BusinessBrains/businessBrains.jsx contains `w-4/5 mx-auto mt-8 mb-8` on the wrapper div
    - src/components/BusinessBrains/businessBrains.jsx contains `className="mb-8"` on both iframes
    - Both Spotify episode src URLs are preserved exactly: `3uqsIqXuVVHPeMTlB5FQ1i` (Pod 2) and `0eoHeo60heCHlVDOHZOug7` (Pod 1)
    - Both iframes retain `width="100%"` and `height="232"` as HTML attributes
    - `frameBorder` is camelCase on both iframes
    - `allowTransparency={true}` is camelCase (NOT lowercase `allowtransparency`) on both iframes
    - src/components/BusinessBrains/businessBrains.css does NOT exist on disk
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>businessBrains.jsx is a functional Tailwind component with 80%-wide wrapper, per-iframe mb-8 spacing, and correct camelCase JSX attributes; businessBrains.css is deleted; build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→external iframe | Spotify embed URLs load third-party content in sandboxed iframes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Information Disclosure | Spotify iframes | accept | Spotify podcast embeds are public content; no PII crosses this boundary. The `allow="encrypted-media"` attribute is the minimum required for Spotify embeds to function. `allowTransparency` is a visual rendering hint with no security implication. |
| T-07-06 | Tampering | businessBrains.jsx static render | accept | Component is render-only with no user input, no state mutation, and no dynamic URL construction. Both src URLs are hardcoded string literals with fixed episode IDs. |
</threat_model>

<verification>
- Navigate to /business-brains route in dev server: two Spotify podcast iframes visible inside a centred 80%-wide wrapper, title in gold
- Each iframe has visible spacing below it
- No React warnings in console about unknown DOM attribute `allowtransparency`
- No console errors about missing CSS imports
- `npm run build` exits 0 with no warnings
</verification>

<success_criteria>
- businessBrains.jsx is a functional component with no class component syntax
- Both Spotify episode URLs preserved exactly
- Wrapper div uses `w-4/5 mx-auto mt-8 mb-8` (per D-03)
- Each iframe uses `className="mb-8"` with `width="100%" height="232"` as HTML attributes (per D-04)
- `allowTransparency={true}` is camelCase — fixing the pre-existing lowercase JSX warning
- Title uses `text-accent text-center text-2xl font-semibold mb-4` (per D-05)
- businessBrains.css deleted (per D-07)
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-remaining-pages/07-03-SUMMARY.md`
</output>
