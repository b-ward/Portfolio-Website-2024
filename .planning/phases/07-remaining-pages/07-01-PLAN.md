---
phase: 07-remaining-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Photos/photos.jsx
  - src/components/Photos/photos.css
autonomous: true
requirements:
  - PAGE-01

must_haves:
  truths:
    - "The Photos page renders its five YouTube iframes in a 16:9 aspect ratio, centred at 60% width"
    - "The page title reads 'Videography' in gold (#ffc200) and is centred"
    - "The subtitle is visible in white text, centred below the title"
    - "No Bootstrap or custom CSS classes exist in the rendered component"
    - "photos.css no longer exists in the repository"
  artifacts:
    - path: "src/components/Photos/photos.jsx"
      provides: "Functional Photos component with Tailwind classes"
      contains: "const Photos"
    - path: "src/components/Photos/photos.css"
      provides: "CSS file must be DELETED"
  key_links:
    - from: "src/components/Photos/photos.jsx"
      to: "src/index.css @theme tokens"
      via: "text-accent Tailwind utility class"
      pattern: "text-accent"
---

<objective>
Convert photos.jsx from a Bootstrap-free class component to a functional component with Tailwind CSS, fix the iframe aspect ratio from 3:2 to correct 16:9, and delete photos.css.

Purpose: Completes the Tailwind migration for the Photos page — the last class component with a custom CSS file in this path.
Output: Functional photos.jsx with Tailwind classes, photos.css deleted.
</objective>

<execution_context>
@C:/Users/bward/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bward/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-remaining-pages/07-CONTEXT.md
@.planning/phases/07-remaining-pages/07-PATTERNS.md

<interfaces>
<!-- Tailwind theme tokens from src/index.css -->
<!-- text-accent = #ffc200, bg-bg = #242424, bg-surface = #333134 -->
<!-- pt-14 is already on <main> in App.jsx — no top margin needed on component wrappers -->

<!-- Title pattern established in prior phases (projects.jsx, cv.jsx): -->
<!-- <h3 className="text-accent text-center text-2xl font-semibold mb-4"> -->

<!-- Functional component pattern established in Phase 3-6: -->
<!-- const Foo = () => ( <div>...</div> ); export default Foo; -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite photos.jsx as functional Tailwind component and delete photos.css</name>
  <files>src/components/Photos/photos.jsx, src/components/Photos/photos.css</files>
  <read_first>
    - src/components/Photos/photos.jsx — current class component to be replaced
    - src/components/Photos/photos.css — CSS file to confirm it exists before deletion
    - .planning/phases/07-remaining-pages/07-CONTEXT.md — D-01 through D-07 decisions
    - .planning/phases/07-remaining-pages/07-PATTERNS.md — exact Tailwind class strings per decision
  </read_first>
  <action>
Overwrite src/components/Photos/photos.jsx with the following content exactly (per D-01 functional conversion, D-02 iframe sizing, D-05 title, D-07 CSS deletion):

```jsx
import React from 'react';

const Photos = () => (
  <div>
    <h3 className="text-accent text-center text-2xl font-semibold mb-4">Videography</h3>
    <p className="text-center text-white">Make sure to change the quality to 1080p/4k</p>
    <iframe
      className="aspect-video w-3/5 mx-auto block mb-8"
      title="Northern Territory 2021"
      src="https://www.youtube.com/embed/z_7wfXeZL-g?si=2c-qHQHLI3aRFfkB"
      allow="fullscreen;"
    ></iframe>
    <iframe
      className="aspect-video w-3/5 mx-auto block mb-8"
      title="Northern Territory 2021"
      src="https://www.youtube.com/embed/0yzM_pxB0qY"
      allow="fullscreen;"
    ></iframe>
    <iframe
      className="aspect-video w-3/5 mx-auto block mb-8"
      title="Europe 19/20"
      src="https://www.youtube.com/embed/QTkTdYfBvCk"
      allow="fullscreen;"
    ></iframe>
    <iframe
      className="aspect-video w-3/5 mx-auto block mb-8"
      title="Europe 18/19"
      src="https://www.youtube.com/embed/Y9x_vyCl0lg"
      allow="fullscreen;"
    ></iframe>
    <iframe
      className="aspect-video w-3/5 mx-auto block mb-8"
      title="South-East Asia 17/18"
      src="https://www.youtube.com/embed/p_jTXvdzkHM"
      allow="fullscreen;"
    ></iframe>
  </div>
);

export default Photos;
```

Then delete src/components/Photos/photos.css (the import line was already removed by writing the new file above — confirm no `import './photos.css'` exists in the new photos.jsx).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('src/components/Photos/photos.jsx','utf8'); if(c.includes('class Photos')) throw new Error('class component not removed'); if(c.includes('photos.css')) throw new Error('CSS import not removed'); if(!c.includes('const Photos')) throw new Error('functional component missing'); if(!c.includes('text-accent')) throw new Error('text-accent missing'); if(!c.includes('aspect-video')) throw new Error('aspect-video missing'); if(!c.includes('w-3/5')) throw new Error('w-3/5 missing'); if(c.includes('photos-title')) throw new Error('old CSS class still present'); console.log('PASS');"</automated>
    Check photos.css was deleted: `node -e "const fs=require('fs'); if(fs.existsSync('src/components/Photos/photos.css')) throw new Error('photos.css still exists'); console.log('PASS');"`
    Check build: `npm run build` exits 0
  </verify>
  <acceptance_criteria>
    - src/components/Photos/photos.jsx contains `const Photos` (functional component)
    - src/components/Photos/photos.jsx does NOT contain `class Photos`
    - src/components/Photos/photos.jsx does NOT contain `import './photos.css'`
    - src/components/Photos/photos.jsx does NOT contain `photos-title`, `photos-subtitle`, or `video` as className values
    - src/components/Photos/photos.jsx contains `text-accent text-center text-2xl font-semibold mb-4`
    - src/components/Photos/photos.jsx contains `aspect-video w-3/5 mx-auto block mb-8` on all five iframes
    - src/components/Photos/photos.jsx contains `text-center text-white` on the subtitle paragraph
    - All five original iframe src URLs are preserved exactly (z_7wfXeZL-g, 0yzM_pxB0qY, QTkTdYfBvCk, Y9x_vyCl0lg, p_jTXvdzkHM)
    - src/components/Photos/photos.css does NOT exist on disk
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>photos.jsx is a functional Tailwind component with 16:9 iframes; photos.css is deleted; build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→external iframe | YouTube embed URLs load third-party content in sandboxed iframes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | YouTube iframes | accept | YouTube embeds are public content; no PII crosses this boundary. The `allow="fullscreen;"` attribute is minimal permission scope. |
| T-07-02 | Tampering | photos.jsx static render | accept | Component is render-only with no user input, no state mutation, and no dynamic URL construction. All src URLs are hardcoded string literals. |
</threat_model>

<verification>
- Navigate to /photos route in dev server: five YouTube embeds visible, title in gold, centred
- Iframes render at 16:9 ratio (noticeably wider relative to height than before)
- No console errors about missing CSS imports
- `npm run build` exits 0 with no warnings about photos.css or photos-title classes
</verification>

<success_criteria>
- photos.jsx is a functional component with no class component syntax
- All five YouTube src URLs are preserved exactly as in the original
- Tailwind classes `aspect-video w-3/5 mx-auto block mb-8` present on every iframe
- Title uses `text-accent text-center text-2xl font-semibold mb-4` (per D-05)
- Subtitle uses `text-center text-white`
- photos.css deleted (per D-07)
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-remaining-pages/07-01-SUMMARY.md`
</output>
