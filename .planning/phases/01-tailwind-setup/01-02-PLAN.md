---
phase: 01-tailwind-setup
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - vite.config.js
  - src/index.css
  - src/index.jsx
  - src/App.css
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04
  - QUAL-03

must_haves:
  truths:
    - "Tailwind v4 is active: @import 'tailwindcss' is the first line of src/index.css"
    - "Four colour tokens are defined in @theme: --color-bg, --color-surface, --color-accent, --color-text"
    - "No Bootstrap CSS import exists in src/index.jsx"
    - "No .btn-primary Bootstrap override block exists in src/App.css"
    - "npm run build exits with code 0 and produces a dist/ bundle"
    - "npm run dev starts with no CSS import errors"
  artifacts:
    - path: "vite.config.js"
      provides: "@tailwindcss/vite plugin registered"
      contains: "import tailwindcss from '@tailwindcss/vite'"
    - path: "src/index.css"
      provides: "Tailwind import + @theme colour tokens"
      contains: "@import \"tailwindcss\""
    - path: "src/index.jsx"
      provides: "Entry point with Bootstrap import removed"
    - path: "src/App.css"
      provides: "App styles with .btn-primary override removed"
  key_links:
    - from: "vite.config.js"
      to: "node_modules/@tailwindcss/vite"
      via: "plugins: [react(), tailwindcss()]"
      pattern: "tailwindcss\\(\\)"
    - from: "src/index.jsx"
      to: "src/index.css"
      via: "existing import chain"
      pattern: "index\\.css"
    - from: "src/index.css"
      to: "Tailwind CSS engine"
      via: "@import \"tailwindcss\""
      pattern: "@import \"tailwindcss\""
---

<objective>
Apply all source-file edits to activate Tailwind v4 and complete Bootstrap removal.

Purpose: With packages in place (Plan 01), this plan wires the build tool (vite.config.js), replaces index.css content with the Tailwind import + @theme block, removes the Bootstrap CSS import from index.jsx, and removes the now-meaningless .btn-primary override from App.css. After these four surgical edits, npm run build and npm run dev both work cleanly with Tailwind active.

Output: Four modified files; a passing production build; dev server with no CSS import errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Git/Portfolio-Website-2024/.planning/PROJECT.md
@C:/Git/Portfolio-Website-2024/.planning/ROADMAP.md
@C:/Git/Portfolio-Website-2024/.planning/STATE.md
@C:/Git/Portfolio-Website-2024/.planning/phases/01-tailwind-setup/01-01-SUMMARY.md

<interfaces>
<!-- Current state of all four files being modified. Executor reads these before touching anything. -->

vite.config.js — current full content:
```javascript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    host: '127.0.0.1',
    port: 5173,
    strictPort: true,
    proxy: {
      // Proxy requests starting with /sportradar to the Sportradar API
      // Dev only; this avoids CORS errors during local development.
      '/sportradar': {
        target: 'https://api.sportradar.com',
        changeOrigin: true,
        secure: true,
        rewrite: (path) => path.replace(/^\/sportradar/, ''),
      },
    },
  },
})
```

src/index.css — current full content:
```css
:root {
  font-family: Inter, system-ui, Avenir, Helvetica, Arial, sans-serif;
  line-height: 1.5;
  font-weight: 400;

  /* app background variable used by components to fill the viewport */
  --app-background: #242424;
  color-scheme: light dark;
  color: rgba(255, 255, 255, 0.87);
  background-color: var(--app-background);

  font-synthesis: none;
  text-rendering: optimizeLegibility;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

a {
  font-weight: 500;
  color: #646cff;
  text-decoration: inherit;
}
a:hover {
  color: #535bf2;
}

html, body, #root {
  height: 100%;
  /* make the document background the same app background to avoid gaps */
  background-color: var(--app-background, #242424);
}

body {
  margin: 0;
  padding: 0;
  min-height: 100vh;
  display: flex;
  /* keep layout starting at the top so background fills the viewport */
  align-items: flex-start;
}

h1 {
  font-size: 3.2em;
  line-height: 1.1;
}

button {
  border-radius: 8px;
  border: 1px solid transparent;
  padding: 0.6em 1.2em;
  font-size: 1em;
  font-weight: 500;
  font-family: inherit;
  background-color: #1a1a1a;
  cursor: pointer;
  transition: border-color 0.25s;
}
button:hover {
  border-color: #646cff;
}
button:focus,
button:focus-visible {
  outline: 4px auto -webkit-focus-ring-color;
}

@media (prefers-color-scheme: light) {
  :root {
    color: #213547;
    /* allow light preference to change app background */
    --app-background: #ffffff;
    background-color: var(--app-background);
  }
  a:hover {
    color: #747bff;
  }
  button {
    background-color: #f9f9f9;
  }
}
```

src/index.jsx — current full content:
```javascript
import React from 'react';
import { createRoot } from 'react-dom/client'
import App from './App';
import 'bootstrap/dist/css/bootstrap.min.css';

createRoot(document.getElementById('root')).render(
    <App />,
);
```

src/App.css — current full content (lines 1–52):
```css
.App {
  text-align: center;
}

.Layout {
  background-color: none;
  display: flex;
  flex-direction: row;
}
.app-background {
  min-height: 100vh;
  background-color: var(--app-background, #333134);
}

.page-content {
  background-color: var(--app-background, #333134);
  width: 100%;
  min-height: calc(100vh - 72px);
  box-sizing: border-box;
}

/* Bootstrap button colour */
.btn-primary, .btn-primary:hover, .btn-primary:active, .btn-primary:visited {
  background-color: #ffc200 !important;
  border-color: #ffc200 !important;
}

.ball {
  position: absolute;
  border-radius: 100%;
  opacity: 0.7;
  z-index: -1;
}

.App::-webkit-scrollbar {
  display: none;
}

/* .circle { ... } */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register Tailwind Vite plugin in vite.config.js</name>
  <files>vite.config.js</files>
  <read_first>
    - C:/Git/Portfolio-Website-2024/vite.config.js — current state is shown in the interfaces block above; read before editing to confirm it still matches
  </read_first>
  <action>
Make exactly two changes to vite.config.js:

1. Add a third import statement on line 3 (after the existing two imports):
   `import tailwindcss from '@tailwindcss/vite'`

2. Expand the plugins array from `[react()]` to `[react(), tailwindcss()]`

React plugin MUST come first in the array — it handles JSX transforms which must run before Tailwind's CSS scanning.

The server/proxy block is NOT touched. Leave it exactly as-is.

The complete target file:
```javascript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react(), tailwindcss()],
  server: {
    host: '127.0.0.1',
    port: 5173,
    strictPort: true,
    proxy: {
      // Proxy requests starting with /sportradar to the Sportradar API
      // Dev only; this avoids CORS errors during local development.
      '/sportradar': {
        target: 'https://api.sportradar.com',
        changeOrigin: true,
        secure: true,
        rewrite: (path) => path.replace(/^\/sportradar/, ''),
      },
    },
  },
})
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('vite.config.js','utf8');if(!c.includes(\"import tailwindcss from '@tailwindcss/vite'\"))process.exit(1);if(!c.includes('react(), tailwindcss()'))process.exit(1);console.log('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - `vite.config.js` contains the line `import tailwindcss from '@tailwindcss/vite'`
    - `vite.config.js` contains `plugins: [react(), tailwindcss()]` (react first, then tailwindcss)
    - The server/proxy configuration block is unchanged (host, port, proxy target still present)
    - The verify node command exits 0 and prints "PASS"
  </acceptance_criteria>
  <done>vite.config.js registers the @tailwindcss/vite plugin after the React plugin.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite src/index.css — Tailwind import, @theme tokens, remove Bootstrap leftovers</name>
  <files>src/index.css</files>
  <read_first>
    - C:/Git/Portfolio-Website-2024/src/index.css — current state is shown in the interfaces block above; read before editing to confirm it still matches
  </read_first>
  <action>
Replace the ENTIRE content of src/index.css with the following. Every line matters — do not approximate.

Changes being made (per decisions D-03 to D-14):
- Prepend `@import "tailwindcss"` as line 1 — MUST be first, CSS spec requires @import before all other rules (D-03)
- Add `@theme` block with four colour tokens immediately after import (D-04)
- Remove `--app-background` CSS variable from :root (D-05)
- Change `background-color: var(--app-background)` to `background-color: #242424` in :root (D-05)
- Change `color-scheme: light dark` to `color-scheme: dark` (D-11)
- Remove `a` and `a:hover` rules — Vite default link colours not needed (D-14)
- Change `background-color: var(--app-background, #242424)` to `background-color: #242424` in html,body,#root (D-13)
- Remove entire `@media (prefers-color-scheme: light)` block — dark-only site (D-10)
- Retain font stack, line-height, font-rendering settings (D-12)
- Retain html/body/#root height:100%, body margin:0 resets (D-13)

Complete target file content (write this verbatim):
```css
@import "tailwindcss";

@theme {
  --color-bg: #242424;
  --color-surface: #333134;
  --color-accent: #ffc200;
  --color-text: #ffffff;
}

:root {
  font-family: Inter, system-ui, Avenir, Helvetica, Arial, sans-serif;
  line-height: 1.5;
  font-weight: 400;

  color-scheme: dark;
  color: rgba(255, 255, 255, 0.87);
  background-color: #242424;

  font-synthesis: none;
  text-rendering: optimizeLegibility;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

html, body, #root {
  height: 100%;
  background-color: #242424;
}

body {
  margin: 0;
  padding: 0;
  min-height: 100vh;
  display: flex;
  align-items: flex-start;
}

h1 {
  font-size: 3.2em;
  line-height: 1.1;
}

button {
  border-radius: 8px;
  border: 1px solid transparent;
  padding: 0.6em 1.2em;
  font-size: 1em;
  font-weight: 500;
  font-family: inherit;
  background-color: #1a1a1a;
  cursor: pointer;
  transition: border-color 0.25s;
}
button:hover {
  border-color: #646cff;
}
button:focus,
button:focus-visible {
  outline: 4px auto -webkit-focus-ring-color;
}
```

Note on `--color-text: #ffffff` naming: this generates the utility `text-text` which is unusual but valid. It is named per D-04 which specifies the semantic purpose. Rename to `--color-foreground` is within Claude's discretion per CONTEXT.md — either name is acceptable; `--color-text` is used here to match the locked decision exactly.

Note on `--app-background` removal: safe to remove. The grep in PATTERNS.md confirmed that the only usages of `var(--app-background)` in src/ are in index.css itself and App.css. The App.css usages both carry a #333134 fallback and will not regress visually when the variable is removed.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('src/index.css','utf8');const checks=[c.startsWith('@import \"tailwindcss\"'),c.includes('@theme'),c.includes('--color-bg: #242424'),c.includes('--color-surface: #333134'),c.includes('--color-accent: #ffc200'),c.includes('--color-text: #ffffff'),!c.includes('--app-background'),!c.includes('prefers-color-scheme: light'),!c.includes('color: #646cff'),c.indexOf('@import')===0];const labels=['starts-with-import','has-@theme','color-bg','color-surface','color-accent','color-text','no-app-background','no-light-mode','no-vite-link-color','import-is-first'];checks.forEach((ok,i)=>{if(!ok){console.error('FAIL:',labels[i]);process.exit(1)}});console.log('PASS: all checks passed')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/index.css` begins with `@import "tailwindcss"` on line 1 (no preceding content, no blank line before it)
    - `@theme` block is present and contains all four tokens: `--color-bg: #242424`, `--color-surface: #333134`, `--color-accent: #ffc200`, `--color-text: #ffffff`
    - `--app-background` does NOT appear anywhere in the file
    - `@media (prefers-color-scheme: light)` block does NOT appear anywhere in the file
    - The `a { color: #646cff }` and `a:hover` rules are NOT present
    - `color-scheme: dark` is present in :root (not `light dark`)
    - `background-color: #242424` appears as a literal value (not via CSS var) in both :root and html/body/#root
    - Font stack (`Inter, system-ui, Avenir`) is retained in :root
    - `height: 100%` is retained on html, body, #root
    - The verify node command exits 0 and prints "PASS: all checks passed"
  </acceptance_criteria>
  <done>src/index.css contains the Tailwind v4 import, all four @theme colour tokens, and has had all Bootstrap-era and light-mode CSS removed. Dark-only global styles are in place.</done>
</task>

<task type="auto">
  <name>Task 3: Remove Bootstrap import from src/index.jsx and .btn-primary override from src/App.css; verify build</name>
  <files>src/index.jsx, src/App.css</files>
  <read_first>
    - C:/Git/Portfolio-Website-2024/src/index.jsx — current state shown in interfaces block above
    - C:/Git/Portfolio-Website-2024/src/App.css — current state shown in interfaces block above
  </read_first>
  <action>
**Edit 1: src/index.jsx** (per D-07, QUAL-03 partial)

Delete the Bootstrap CSS import line. No replacement is needed — Tailwind is activated via src/index.css which is already in the module graph.

Target state:
```javascript
import React from 'react';
import { createRoot } from 'react-dom/client'
import App from './App';

createRoot(document.getElementById('root')).render(
    <App />,
);
```

The blank line between the imports and the createRoot call can be kept or removed — either is acceptable.

**Edit 2: src/App.css** (per D-08)

Delete the Bootstrap button colour override block. This is the comment plus the rule — 5 lines total:
```css
/* Bootstrap button colour */
.btn-primary, .btn-primary:hover, .btn-primary:active, .btn-primary:visited {
  background-color: #ffc200 !important;
  border-color: #ffc200 !important;
}
```

IMPORTANT: Do NOT touch any other rules in App.css. In particular:
- `.app-background` and `.page-content` both use `var(--app-background, #333134)` with a fallback — leave them unchanged. The fallback value (#333134) takes over silently after the variable is removed from index.css. These rules are replaced in Phase 2.
- `.ball`, `.App`, `.Layout` — all retained unchanged.

**After both edits, run the build gate:**

```bash
npm run build
```

This must exit 0 (no errors). A successful build confirms:
- Tailwind v4 processes index.css without errors (SETUP-02, SETUP-03)
- No CSS import errors from removed Bootstrap package (SETUP-01)
- Production bundle is generated (SETUP-04)

**Expected acceptable warnings:** Routes that import react-bootstrap (about, cv, projects, trio, trainGame) will show `Cannot find module 'react-bootstrap'` warnings or errors at runtime when visited. This is expected per D-09 and does NOT constitute a build failure. The build itself must exit 0.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const jsx=fs.readFileSync('src/index.jsx','utf8');const css=fs.readFileSync('src/App.css','utf8');if(jsx.includes('bootstrap/dist/css/bootstrap.min.css')){console.error('FAIL: bootstrap import still in index.jsx');process.exit(1)}if(css.includes('.btn-primary')){console.error('FAIL: .btn-primary still in App.css');process.exit(1)}console.log('PASS: both files clean')" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/index.jsx` does NOT contain `bootstrap/dist/css/bootstrap.min.css`
    - `src/App.css` does NOT contain `.btn-primary`
    - `src/App.css` does NOT contain `/* Bootstrap button colour */`
    - `src/App.css` still contains `.app-background`, `.page-content`, `.ball`, `.Layout` (unchanged)
    - `npm run build` exits with code 0
    - A `dist/` directory is created containing built assets
    - The verify node command prints "PASS: both files clean"
  </acceptance_criteria>
  <done>Bootstrap CSS import removed from index.jsx (QUAL-03 partial — only Bootstrap CSS import is gone; per-component CSS files are untouched per D-09). .btn-primary override removed from App.css. Production build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS cascade | index.css is the global stylesheet; changes here affect all routes |
| Build pipeline | vite.config.js controls what transforms run; incorrect plugin order can break JSX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | vite.config.js plugin order | mitigate | react() must come before tailwindcss() in plugins array — JSX transform must run before CSS scanning. Acceptance criteria verifies the exact order `react(), tailwindcss()`. |
| T-02-02 | Denial of Service | @import placement in index.css | mitigate | `@import "tailwindcss"` must be on line 1 per CSS spec. Placing it after other rules can break cascade ordering. Acceptance criteria verifies the file starts with the import. |
| T-02-03 | Information Disclosure | --app-background CSS variable removal | accept | Confirmed safe: grep in PATTERNS.md verified the only usages of var(--app-background) in src/ are in index.css and App.css, both with #333134 fallbacks. No per-component CSS files reference it. |
</threat_model>

<verification>
Full phase gate after Plan 02 completes:

1. Grep checks (automated):
```bash
node -e "const fs=require('fs');const p=require('./package.json');const idx=fs.readFileSync('src/index.css','utf8');const jsx=fs.readFileSync('src/index.jsx','utf8');const css=fs.readFileSync('src/App.css','utf8');const vc=fs.readFileSync('vite.config.js','utf8');const checks={'no-bootstrap-dep':!p.dependencies?.bootstrap&&!p.dependencies?.['react-bootstrap'],'tailwind-devdep':!!p.devDependencies?.tailwindcss,'vite-plugin-devdep':!!p.devDependencies?.['@tailwindcss/vite'],'vite-plugin-import':vc.includes(\"import tailwindcss from '@tailwindcss/vite'\"),'vite-plugin-registered':vc.includes('react(), tailwindcss()'),'tailwind-import-first':idx.startsWith('@import \"tailwindcss\"'),'theme-tokens':idx.includes('--color-bg')&&idx.includes('--color-surface')&&idx.includes('--color-accent')&&idx.includes('--color-text'),'no-app-background-in-index':!idx.includes('--app-background'),'no-light-mode':!idx.includes('prefers-color-scheme: light'),'no-bootstrap-import-in-jsx':!jsx.includes('bootstrap/dist/css'),'no-btn-primary':!css.includes('.btn-primary')};let fail=false;Object.entries(checks).forEach(([k,v])=>{if(!v){console.error('FAIL:',k);fail=true}});if(!fail)console.log('ALL CHECKS PASSED')"
```

2. Build gate:
```bash
npm run build
```
Must exit 0.

3. Lint gate:
```bash
npm run lint
```
Must produce no errors (warnings from react-bootstrap-importing files are acceptable — those are runtime errors only, not lint errors).

4. Manual dev-server check (checkpoint step — see VALIDATION.md):
- Run `npm run dev`
- Open http://127.0.0.1:5173 in browser
- Open DevTools → Elements → Styles
- Confirm Tailwind CSS utilities (e.g., any element with a Tailwind class applied) are resolving correctly
- Confirm no CSS import errors in terminal output
- Note: Visiting /about, /cv, /projects, /trio, /train routes WILL show react-bootstrap module errors — this is expected per D-09
</verification>

<success_criteria>
- `@import "tailwindcss"` is line 1 of src/index.css
- @theme block defines all four tokens: --color-bg #242424, --color-surface #333134, --color-accent #ffc200, --color-text #ffffff (per D-04)
- SETUP-02: @theme tokens are present and verifiable via grep (ROADMAP success criteria 2 — note: no tailwind.config.js is created; v4 CSS-first approach satisfies SETUP-02 per D-02/D-03)
- SETUP-01: bootstrap and react-bootstrap absent from package.json dependencies; Bootstrap CSS import absent from index.jsx
- SETUP-03: npm run dev starts without CSS import errors
- SETUP-04: npm run build exits 0
- QUAL-03 (partial): The only Bootstrap CSS file import (`bootstrap/dist/css/bootstrap.min.css`) is removed from index.jsx. The 19 per-component CSS files are untouched per D-09 — full QUAL-03 satisfaction requires all 8 phases.
- No postcss.config.js or tailwind.config.js created
</success_criteria>

<output>
After completion, create `.planning/phases/01-tailwind-setup/01-02-SUMMARY.md` following the summary template.
</output>
