---
phase: 01-tailwind-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - SETUP-01

must_haves:
  truths:
    - "bootstrap and react-bootstrap are absent from node_modules and package.json"
    - "tailwindcss and @tailwindcss/vite are present as devDependencies"
  artifacts:
    - path: "package.json"
      provides: "Updated dependency manifest"
      contains: "tailwindcss"
    - path: "node_modules/tailwindcss"
      provides: "Tailwind v4 package installed on disk"
    - path: "node_modules/@tailwindcss/vite"
      provides: "Vite plugin installed on disk"
  key_links:
    - from: "vite.config.js"
      to: "node_modules/@tailwindcss/vite"
      via: "import tailwindcss from '@tailwindcss/vite'"
      pattern: "tailwindcss"
---

<objective>
Swap Bootstrap out for Tailwind in the npm dependency tree.

Purpose: Bootstrap must be uninstalled before build tooling is updated — the vite.config.js in Plan 02 imports @tailwindcss/vite, which must already exist in node_modules for the build to succeed. Separating the npm step into its own wave makes the dependency order explicit and keeps each plan's verification clean.

Output: package.json with bootstrap and react-bootstrap removed from dependencies, tailwindcss and @tailwindcss/vite added as devDependencies, node_modules updated accordingly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Git/Portfolio-Website-2024/.planning/PROJECT.md
@C:/Git/Portfolio-Website-2024/.planning/ROADMAP.md
@C:/Git/Portfolio-Website-2024/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Uninstall Bootstrap, install Tailwind v4 packages</name>
  <files>package.json</files>
  <read_first>
    - C:/Git/Portfolio-Website-2024/package.json — read the full file before running any commands so you know the exact current state of dependencies and devDependencies
  </read_first>
  <action>
Run these two commands in order from the project root (C:/Git/Portfolio-Website-2024):

```
npm uninstall bootstrap react-bootstrap
npm install -D tailwindcss @tailwindcss/vite
```

Both packages are official Tailwind Labs packages — exact names matter to avoid typosquats (per ASVS L1 supply-chain concern T-01-01).

Expected result after both commands:
- package.json `dependencies` no longer contains `"bootstrap"` or `"react-bootstrap"`
- package.json `devDependencies` contains `"tailwindcss": "^4.x.x"` and `"@tailwindcss/vite": "^4.x.x"`
- node_modules/tailwindcss and node_modules/@tailwindcss/vite both exist

Do NOT create postcss.config.js. Do NOT create tailwind.config.js. Tailwind v4 needs neither.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const hasBs=p.dependencies?.bootstrap||p.devDependencies?.bootstrap||p.dependencies?.['react-bootstrap']||p.devDependencies?.['react-bootstrap']; const hasTw=p.devDependencies?.tailwindcss&&p.devDependencies?.['@tailwindcss/vite']; if(hasBs){console.error('FAIL: bootstrap still present');process.exit(1)} if(!hasTw){console.error('FAIL: tailwind packages missing');process.exit(1)} console.log('PASS: dependencies correct')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` has no `"bootstrap"` key anywhere (neither dependencies nor devDependencies)
    - `package.json` has no `"react-bootstrap"` key anywhere (neither dependencies nor devDependencies)
    - `package.json` devDependencies contains `"tailwindcss"` with a ^4.x.x version
    - `package.json` devDependencies contains `"@tailwindcss/vite"` with a ^4.x.x version
    - The verify command above exits 0 and prints "PASS: dependencies correct"
    - `node_modules/@tailwindcss/vite` directory exists on disk
  </acceptance_criteria>
  <done>npm dependency tree has been updated: Bootstrap removed, Tailwind v4 installed. The verify node command exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer workstation → npm registry | Third-party packages installed from npm; malicious package via typosquat is the primary risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm install step | mitigate | Use exact official package names: `tailwindcss` (not `tailwind-css`) and `@tailwindcss/vite` (not `@tailwindcss/vite-plugin`). Both are published by Tailwind Labs. Verify with `npm view tailwindcss` before install if in doubt. |
| T-01-02 | Tampering | node_modules integrity | accept | npm lockfile (package-lock.json) pins exact resolved versions and checksums. Risk is low for a well-known framework package with millions of downloads. |
</threat_model>

<verification>
After Task 1 completes:

```bash
node -e "const p=require('./package.json'); const hasBs=p.dependencies?.bootstrap||p.devDependencies?.bootstrap||p.dependencies?.['react-bootstrap']||p.devDependencies?.['react-bootstrap']; const hasTw=p.devDependencies?.tailwindcss&&p.devDependencies?.['@tailwindcss/vite']; if(hasBs){console.error('FAIL: bootstrap still present');process.exit(1)} if(!hasTw){console.error('FAIL: tailwind packages missing');process.exit(1)} console.log('PASS: dependencies correct')"
```

Must print "PASS: dependencies correct" and exit 0.
</verification>

<success_criteria>
- bootstrap and react-bootstrap are absent from package.json and node_modules
- tailwindcss@^4.x.x and @tailwindcss/vite@^4.x.x are present in devDependencies
- No postcss.config.js or tailwind.config.js was created
</success_criteria>

<output>
After completion, create `.planning/phases/01-tailwind-setup/01-01-SUMMARY.md` following the summary template.
</output>
